📖 このCHANGELOGと連動した開発の進捗・雑感は [開発日記](https://ocoe-puipui.github.io/diary/) でも公開しています。

## 目次（更新日順）

* [2026-08-16](#--2026-08-16)
* [2026-08-15](#--2026-08-15)
* [2026-08-14](#--2026-08-14)
* [2026-08-09](#--2026-08-09)
* [2026-08-05](#--2026-08-05)
* [2026-08-04](#--2026-08-04)
* [2026-08-03(2)](#--2026-08-032)
* [2026-08-03](#--2026-08-03)
* [2026-07-30](#--2026-07-30)
* [2026-07-29](#--2026-07-29)
* [2026-07-28](#--2026-07-28)
* [2026-07-26](#--2026-07-26)
* [2026-07-25](#--2026-07-25)
* [2026-07-23](#--2026-07-23)
* [2026-07-22](#--2026-07-22)
* [2026-07-21](#--2026-07-21)
* [2026-07-20](#--2026-07-20)
* [2026-07-19](#--2026-07-19)

---

## - 2026-08-16
---
### RELEASE
* **v1.2.0** をこの時点のコードでリリース（メモ欄・EXIF情報表示・フォルダ専用の自動採番・一括リネームと一括で書き出すの分離・プレースホルダー機能の改善などを含む）

### ADDED

## 上部バー左端にバージョン番号表示を追加（v1.3.0向け、開発中）
* アプリ名仮表示ラベルの撤去に伴い空いていた上部バー左端に、控えめなバージョン番号表示（例: `v1.3.0`）を追加。問い合わせ・不具合報告時にバージョンを確認しやすくするため
* バージョン番号を`version.py`の`APP_VERSION`に一元化し、`app.py`（画面表示）と`AIImageViewer.spec`（`CFBundleShortVersionString`/`CFBundleVersion`）の両方から参照する形にした（リリースごとに1箇所を更新するだけで両方に反映される）

## アプリ名を「Seed Book」に正式決定（v1.3.0向け、開発中）
* これまで「AI Image Viewer & Manager（仮称: Seed Book）」としていたアプリ名を、「Seed Book（AI image browser）」に正式決定
* Dock・Cmd+Tab表示名（CFBundleName）／ウィンドウタイトル／.appファイル名（`Seed Book.app`）／DMGボリューム名／アプリ内ヘルプ本文冒頭／README各言語版・マニュアルのタイトル見出しを、すべて新名称に統一
* `.spec`内のバンドル内部の実行ファイル名（EXE/COLLECTの`name=`）は、ターミナル・スクリプトでの扱いやすさを考慮し、スペースなしの`SeedBook`とした（Finder上に表示される`.app`名には影響しない）
* 上部バー左端にあった、名称検討中の仮表示ラベル（v1.1.0で追加）は、名称確定に伴いウィジェットごと撤去
* Bundle Identifierは、開発用（現行）リポジトリでは引き続き`com.ocoe-puipui.aiimageviewer`のまま。配布用リポジトリでは`com.ocoe-puipui.seedbook`を使用する方針（既に決定済み）

## 表示言語切り替え（日本語／English）に対応（v1.3.0向け、開発中）
* 設定画面に「表示言語」項目を追加。「自動（OSの言語設定に追従）」「日本語」「English」から選択可能（変更は次回起動時から反映、アプリ内での動的な切り替えは行わない）
* 外部翻訳ライブラリには依存せず、Python辞書（`i18n.py`）で文言を管理する自前方式を採用（対応言語が日本語・英語の2言語のみのスコープでは、ライセンス表記の追加やビルド成果物の増量が発生しない点、既存の「アイコンをSVG文字列としてコードに埋め込む」設計思想と一貫性がある点を踏まえた判断）
* メイン画面・設定画面・各種サブダイアログ（同期履歴、連番リネームプレビュー、フォルダ取り込み順序、CSV書き出しオプション、フォルダ命名ルール、フォルダ並び順、データベース未検出時の選択ダイアログ、読書モード、フルスクリーンプレビュー）・アプリ内ヘルプ本文の表示文言を英語UI対応させた（日英で360件超のキーを整備）
* 自動採番のプレフィックス／アペンドで使うプレースホルダー（`{フォルダ名}`/`{日付}`）について、英語UI向けの表記（`{folder name}`/`{date}`）を追加。表示言語に関わらずどちらの表記も同じ意味として解決されるため、UI言語をまたいで命名ルールを共有しても壊れない
* ユーザーマニュアルを言語別に分離：`manual.md`を英語版、新規に`manual.ja.md`を日本語版とし、互いにリンクする形に変更（README各言語版・アプリ内ヘルプのマニュアルリンクも、表示言語に応じて参照先を自動的に切り替えるよう対応）

## メモ欄の配置位置を変更（使用モデル欄の上へ）
* メモ欄を「その他パラメータ / メタデータ / EXIF情報」欄の下から、「使用モデル / チェックポイント」欄の上へ移動

## 自動採番プレースホルダーの挿入先修正（プレフィックス欄にも転記できるように）
* プレースホルダー（{日付}/{フォルダ名}）のリンクをクリックした際、常にアペンド欄へ挿入されてしまっていた不具合を修正。プレフィックス欄にカーソルがある状態でクリックした場合は、プレフィックス欄へ挿入されるようにした（リンククリック時点で既にフォーカスがリンク側へ移ってしまうため、直近のフォーカス欄をeventFilterで記録して判定する方式に変更）
* プレフィックス欄への挿入時は、区切りの「_」を後ろに付ける形（例: `{フォルダ名}_`）に変更。アペンド欄への挿入時は従来通り前に付ける（例: `_{日付}`）

## メモ欄を固定表示からスクロール追従に変更（ボタン位置ズレの根本修正）
* メモ欄は保存ボタン行の上に固定表示していたが、スクロールバーの表示有無によって他のメタデータ欄とボタン位置が揃わなくなる問題があったため、固定表示をやめ、「その他パラメータ / メタデータ / EXIF情報」欄の下に、他の編集欄と同じくスクロールエリア内に配置するよう変更（一緒にスクロールする）
* これにより、表示/非表示・コピー・クリアの3ボタンの位置は常に他の編集欄と縦一直線に揃うようになった
* メモ欄の見出し行を、他のメタデータ欄とまったく同じ create_section_header() で生成するよう変更（create_section_header / copy_text_to_clipboard をQLineEditにも対応させて共通化）
* 最上段アイコンの並びを修正：メモ欄専用の表示/非表示ボタンは廃止し（一括表示/非表示ボタンに統合）、ヘルプアイコンを設定アイコンのすぐ左に配置

## 画像ごとのメモ欄を追加
* 保存・スライドショー行の直上に、画像ごとの自由記入メモ欄（1行）を追加。プロンプト等のメタデータには影響せず、専用の memo カラムに保存する
* プロンプト等の編集フォームとは異なりスクロールに追従させず、常に保存ボタンの行の上に固定表示
* コピー・クリアボタンに対応。表示/非表示は画面右上（設定アイコンの左）の専用ボタンで切り替え
* 検索の統合インデックスにメモの内容を追加し、検索対象に含めた
* 「別名で保存」時にもメモの内容を新しいコピーへ引き継ぐ

## 取り込み形式チェックボックス欄・メタデータ編集欄に表示/非表示ボタンを追加
* 取り込み対象形式（PNG/JPG/JPEG/WEBP/GIF/BMP/TIFF）のチェックボックス欄の左端に、欄全体を一時的に畳める表示/非表示ボタンを追加（1段のレイアウトを保ったまま追加）
* 画面右上（ヘルプアイコンの左）に、メタデータ編集欄4つ（使用モデル/プロンプト/ネガティブプロンプト/その他パラメータ）と取り込み形式欄を、まとめて表示/非表示に切り替えられるボタンを追加

## カメラ写真のEXIF情報を「その他パラメータ / メタデータ」欄に表示（欄名を「その他パラメータ / メタデータ / EXIF情報」に変更）
* AI生成パラメータが見つからなかった画像（JPEG/TIFF/WEBPなど元々EXIFを持つ形式）について、メーカー・機種・撮影日時・露出時間・F値・ISO感度・焦点距離・GPS座標などのEXIF情報を自動で読み取り、整形して表示するようにした（PNG/GIF/BMPはEXIF非対応のため対象外）
* AI生成パラメータもEXIF情報も見つからない場合は、従来通り「No prompt found」のまま
* 上記の欄名変更に合わせ、詳細エクスポート（CSV）の項目名も「その他パラメータ」→「その他パラメータ/EXIF情報」に変更

## - 2026-08-15
---
### ADDED

## 自動採番のプレフィックス／アペンドにプレースホルダー対応を追加
* `{フォルダ名}`（取り込み元フォルダ名）・`{日付}`（今日の日付、YYYYMMDD）が使えるようになった
* 置換後の文字列を基に採番カウンタが分かれるため、フォルダや日付が変わると自動的に別の連番として管理される
* 設定画面のプレビュー表示も、サンプル値で置換した結果を表示するよう対応
* 一括リネーム・連番コピー等、今後追加する命名系機能でも同じ置換ロジックを共通利用する予定

## 選択画像の一括リネーム機能を追加
* 画像を複数選択して右クリック→「選択画像を一括リネーム（連番）...」で、設定済みの自動採番ルールに沿って実ファイル名・「名前」欄をまとめて連番に変更できるようにした
* 番号はこの操作専用の一時カウンタで1から採番（取り込み時の永続カウンタには影響しない）。プレフィックス／アペンドに{フォルダ名}が含まれる場合はフォルダごとに別々に1から採番される
* 編集ロック中の画像は対象外。実行前に変更後の名前の例を示す確認ポップアップを表示

## 連番リネームコピー機能を追加
* 画像の右クリックメニュー（複数選択可）に「選択画像を一括で書き出す（連番でリネーム）...」を追加。書き出し先フォルダを指定すると、元ファイルはそのままに自動採番ルールで連番リネームしたコピーを作成できるようにした
* フォルダ見出しの右クリックメニューにも「フォルダ内の画像を一括で書き出す（連番でリネーム）...」を追加。そのフォルダ内でDBに登録されている画像だけを対象にコピーする（未登録の紛れ込みファイルは対象外）
* 一括リネーム機能と共通の採番ロジック（`_build_sequence_names`）を使い、番号の振り方・{フォルダ名}対応が一貫するようリファクタリング
* メニュー表記は「コピー」ではなく「一括で書き出す」に統一（そのままの名前で複製するFinderドラッグ&ドロップと区別するため、2026-08-15）

## 設定画面「自動採番・リネーム」グループの表記整理・プレースホルダー挿入リンクを追加
* グループ名を「新規取り込み画像の自動採番ルール」→「自動採番・リネーム」に変更。「自動採番を使用する」チェックボックスも「新規取り込み時に自動採番する」に改称
* 「リネーム時に編集画面を表示する」チェックボックスを新設
* プレフィックス・アペンド欄の横に「プレースホルダー: {日付} {フォルダ名}」のリンクを追加。クリックすると直前にフォーカスしていた欄のカーソル位置に、区切りの「_」を付けた形（例: `_{日付}`）で挿入される
* アペンド欄・プレースホルダーのリンクを桁数欄と別レイアウトに分離し、桁数まわりの間隔が広がってしまう不具合と、アペンド行の縦位置ずれを修正

## 一括リネーム／一括で書き出す実行前の確認・編集画面（SequenceRenamePreviewDialog）を実装
* 「リネーム時に編集画面を表示する」がオンのとき、実行前に「元のファイル名／変更後の名前」の一覧を表示するシンプルな編集ダイアログを開くようにした
* 「変更後の名前」はダブルクリックで直接編集可能。同じ名前・空の名前があると該当行を赤くハイライトし、解消するまで「この内容で実行」を無効化する
* オフの場合は、これまで通り確認ポップアップ（例を数件表示）のみで実行する

## 全画面プレビューの不具合修正・表示調整
* プレビュー表示モードが「非表示」の状態のまま全画面表示にすると、画像が何も表示されない不具合を修正（全画面表示中は常に表示状態にし、解除すると元の「非表示」に戻る）
* 全画面表示時の余白を切り詰め、画像をより大きく表示できるよう調整

## フォルダ見出し行のドラッグ＆ドロップによるフォルダコピー機能を追加
* フォルダ別グループ表示中、見出し行（フォルダアイコン）をFinder等の外部アプリへドラッグ＆ドロップすると、実フォルダごとコピーできるようにした
* プレビュー画像・画像リストの既存ドラッグ機能と同様、常に「コピー」のみを許可し、元のフォルダが移動・削除されることはない

## フォルダ見出し行の右クリックメニューに「フォルダをデータベースから削除」を追加
* フォルダとその中の画像をまとめてデータベースから削除できるようにした（ディスク上の実ファイルは削除しない、DB登録の解除のみ）
* 削除後はそのフォルダが画像リストに表示されなくなり、同期（sync_folders）の対象からも外れる
* 改めて「フォルダを取り込む」で取り込み直せば、再び同期対象に戻る（`folders`テーブルへ再登録されるため）

### FIXED
## 一括リネーム編集画面（SequenceRenamePreviewDialog）の不具合修正
* 「選択画像を一括リネーム」で編集画面を開くとフリーズする不具合を修正。原因は、表セルの直接編集（ダブルクリック編集）に伴う重複チェック処理が`itemChanged`シグナルを再発火させ、自分自身を無限に呼び出していたこと
* セル編集中に文字がほぼ読めなくなる問題も併せて修正。選択行の背景色とセル編集用エディタの配色が噛み合わないことが原因だったため、表セルの直接編集自体を廃止し、行を選択すると下の「名前」欄（他の設定項目と同じ標準的なQLineEdit）に値が読み込まれ、そこで編集した内容が一覧へ反映される方式に変更。フリーズの原因だったセル編集も同時に無くなり、根本的に解消
* あわせて、`QLineEdit`／`QTextEdit`の選択時の配色（`selection-background-color`／`selection-color`）も明示指定するよう変更（アプリ内の他の入力欄にも共通で適用される保険的な修正）
* 「一括で書き出す」の書き出し先フォルダ選択ダイアログが、アプリのソースコードのフォルダを既定で開いていた問題を修正。CSVエクスポートと同じ`get_default_export_dir()`（デスクトップが既定）を使うよう変更
* 同様の問題が他のファイル/フォルダ選択ダイアログにも残っていたため、あわせて修正（バックアップDBファイルの選択、フォルダ取り込み、画像ファイルの個別取り込み、画像を別名で保存の4箇所）。いずれも既定でデスクトップが開くようになった

## フォルダ専用の自動採番機能を追加
* フォルダ見出しの右クリックメニューに「フォルダ専用の自動採番を設定...」を追加。プレフィックス・桁数・アペンドを、そのフォルダだけアプリ全体の既定から上書きできるようにした
* 上書きは新規テーブル`folder_naming_rules`（folder_pathごとに1件）で管理。チェックボックスをオフにすると上書きを解除し、既定ルールに戻る
* このフォルダ専用ルールは、新規取り込み時（`generate_next_sequential_name()`）だけでなく、「選択画像を一括リネーム」「一括で書き出す」（`_build_sequence_names()`）でも優先して適用されるようにした。これにより、既存の保存済み画像も、フォルダ専用ルールに沿ってリネームできる
* メニュー項目の並びを整理: 「フォルダ専用の自動採番を設定...」は「フォルダをデータベースから削除...」の直前（区切り線区切り）に配置。アイコンは並び替えアイコンから書類アイコンに変更（「選択画像を一括リネーム」も同様）
* ダイアログのタイトル・説明文・チェックボックス表記を見直し、チェックボックスはキャンセル/保存ボタンと同じ行（最下部）に配置

## 「一括リネーム」の対象を「名前」欄のみに変更（実ファイル名は変更しない）
* これまで「選択画像を一括リネーム」は実ファイル名（ディスク上）と「名前」欄の両方を変更していたが、DB内でのリネーム・自動採番は「名前」欄のみを対象とする方針に変更し、実ファイル名は変更しないようにした
* 実ファイル名も揃えたい場合は、引き続き「一括で書き出す（連番でリネーム）」を利用する（こちらは従来通り実ファイル名＝拡張子込みが対象）
* 「リネーム時に編集画面を表示する」がオンの場合の確認・編集画面の表記を、一括リネームでは「現在の名前」／「変更後の名前」、一括で書き出すでは「現在のファイル名」／「変更後のファイル名」に分けて、対象の違いが分かりやすいようにした
* 一括リネームの「現在の名前」欄は、実ファイル名ではなくDB上の「名前」欄の原文（大文字小文字そのまま）を表示するよう対応（`QListWidgetItem`に新しいロールを追加）

---

## - 2026-08-14
---
### ADDED

## 同期時の「見つからないフォルダ／画像」「取り込みエラー」を履歴として記録
* これまでは、同期対象フォルダが移動・削除された場合、同期ボタンを押すたびに毎回ポップアップで警告が表示され続けていた（自動的に消える手段がなかった）
* 今回から、同じ問題（フォルダが見つからない・取り込みエラー）が続いている間は、初回のみポップアップで警告し、2回目以降はポップアップを省略して履歴にのみ記録するよう変更
* フォルダが一度でも見つかる状態に戻れば警告状態はリセットされ、その後また見つからなくなった場合は改めてポップアップで警告される（意図的な移動・再接続などを想定）
* 見つからなくなった画像ファイル（同期のたびにDBから自動的に取り除かれるもの）についても、削除のたびに履歴へ記録するようにした
* 同期結果のポップアップに「履歴」ボタンを追加。押すと、これまでに記録された同期履歴（日時・種別・対象パス・内容）を一覧できるダイアログが開く
* 履歴ダイアログ、および設定画面の「同期履歴をエクスポート（CSV）」からも、履歴をCSVファイルとして書き出せるようにした
* 履歴は直近500件まで保持し、それを超えた分は古いものから自動的に削除される

## プレビューエリアでの左右キー送り
* プレビューエリアを一度クリックしてフォーカスを与えると、以後は左右キーで前後の画像に送れるようになった
* 検索欄など他の入力欄から意図せずフォーカスを奪わないよう、常時反応（ホバー）方式ではなく、クリックによる明示的な操作を条件にした

## 上部バーに「パネル反転」「外観モード」のワンクリック切り替えボタンを追加
* ヘルプアイコンの右隣に2つボタンを追加。設定画面を開かずに、左右パネルの配置（標準/反転）と外観モード（自動/ダーク/ライト）を切り替えられる
* パネル反転ボタンは、押すたびに標準⇄反転をトグル。アイコン自体が現在の配置（標準=dock_to_right／反転=dock_to_left）を示す
* 外観モードボタンは、押すたびに「自動→ダーク→ライト→自動…」と巡回。自動（追従）中は専用アイコンを表示し、ダーク/ライト固定中はその見た目のアイコンを表示する
* 設定画面の既存項目（左右パネル配置・外観モード）と設定を共有しており、どちらから変更しても双方に即時反映される

### FIXED
## 同期履歴機能の細かい不具合を修正
* ダークモードで同期履歴ダイアログの文字が背景色と同化して読めなかった問題を修正（QTableWidget用のテーマ配色を追加）
* 同じ内容のエラー・警告が同期のたびに時系列で溜まり続けていた問題を修正。同一内容は最新の1件のみを残すよう変更
* 記録時刻がSQLiteの`CURRENT_TIMESTAMP`（UTC基準）で保存されていたため、日本時間で見ると実際より9時間過去にずれて表示されていた問題を修正（ローカル時刻で記録するよう変更）
* CSVエクスポート（画像一覧・同期履歴）の保存先ダイアログが、アプリの実行時カレントディレクトリという不定な場所を初期表示していた問題を修正。既定でデスクトップを初期表示するよう変更

### CHANGED
## 細かい表示調整
* 「画像一覧をエクスポート」「同期履歴をエクスポート」ボタンの表示を「画像一覧（CSV）」「同期履歴（CSV）」に簡略化し、1行に2個並べる配置に変更
* 自動採番の「桁数」入力欄について、内蔵の上下矢印ボタンが見えづらかったため廃止し、数値入力のみのシンプルな見た目に変更。あわせて範囲の注記「（1〜6桁）」を追加

---

## - 2026-08-09
---
### ADDED

## プレビューエリアの「非表示」表示モード追加
* プレビュー表示サイズ切替ボタン（標準／コンパクト／全画面）の並びに「非表示」ボタンを追加
* 「非表示」を選ぶと、プレビュー画像と前後移動ボタンが隠れ、プレビュー行が占めていた縦スペースがメタデータ編集エリア（プロンプト・ネガティブプロンプト・その他メタデータ）に譲られる
* 意図: メタデータの編集に集中したい時に、編集スペースを広く使えるようにするため

## プレビュー画像のドラッグ＆ドロップによる取り出し機能を追加
* プレビューエリアの画像を、Finder等の外部アプリへ直接ドラッグ＆ドロップして取り出せるようにした（`PreviewLabel`にドラッグ開始処理を実装）
* 画像リストの既存ドラッグ機能と同様、常に「コピー」のみを許可し、誤ってオリジナルファイルが移動して消えることがないようにした

## 左右パネルの配置切替（標準／反転）機能を追加
* 設定画面の「外観モード」グループに、検索・取り込み・リスト等を左右どちらに表示するか選べるラジオボタン（標準／反転）を追加
* 反転を選ぶと、検索・取り込み・ソート・画像リスト・削除ボタンが右側に、プレビュー・メタデータ編集エリアが左側に入れ替わる（右利き/左利きなど、操作しやすい側を選べるようにするため）
* 設定ボタンは、この切替の影響を受けないよう`splitter`の外（ウィンドウ上部）へ移設し、常にウィンドウ右上に固定表示されるようにした

### CHANGED

## 設定画面のデータベースグループのレイアウトを整理
* 「新しいデータベースを作成」ボタンを単独の行から、選択フォーム・「このデータベースに切り替える」ボタンと同じ行へ移動（順序: 選択フォーム → データベースを作成 → データベースを変更する）
* 3つの要素を均等幅で表示するように変更し、見た目を整理
* ボタン表記を「新しいデータベースを作成」→「データベースを作成」、「このデータベースに切り替える」→「データベースを変更する」に変更

## ヘルプボタンをメインウィンドウへ移動、リリースノートボタンを非表示化
* 「ヘルプを表示」ボタンを設定ダイアログからメインウィンドウへ移動し、設定（歯車）ボタンの左に、アイコンのみの小さなボタンとして配置
* 「リリースノート」ボタンは使用頻度が低いため、利用者向けUIとしては非表示に変更（将来的な再利用に備え、ボタン自体とクリック時の処理はコード上に残している）

## ボタン・選択肢の表記を簡略化
* 「データベースを変更する」→「データベースを変更」に変更
* 「毎回確認する」→「毎回確認」、「前回使用した順序を自動選択する」→「前回使用した順序を自動選択」に変更

## プレビュー操作行・保存行を編集エリアのスクロールに追従しないよう修正
* プレビュー表示サイズ切替行（非表示/標準/コンパクト/全画面＋星評価）と、保存・スライドショー操作行を、スクロールエリア（プロンプト等の編集フォーム）の外側へ移動
* プレビューを「非表示」にしてメタデータ編集エリアを広げた状態で上下スクロールしても、これらの行が隠れなくなった

## Seedのコピーボタンを追加、生成パラメータ表示を使用モデル欄の表示/非表示に連動
* 使用モデル欄の下に表示する生成パラメータ（Steps/Sampler/Scheduler/CFG scale/Seed/Size）のうち、Seedの値の右横に小さなコピーボタンを追加
* 生成パラメータ表示行の表示/非表示を、「使用モデル」欄の表示/非表示切替ボタンに連動させた（使用モデル欄を隠すと一緒に隠れ、表示すると一緒に表示される）

## 仮の将来のアプリ名称「Seed Book」をメインウィンドウ上部に表示
* 正式に名称変更するまでの間の仮表示として、上部バー（ヘルプ・設定ボタンと同じ段）の左端に「Seed Book」を表示
* 混乱を避けるため、バージョン番号は表示しない

---

## - 2026-08-05
---
### CHANGED

## dmg配布タスク7: .dmgファイルの作成 完了
* `hdiutil create`により、`AIImageViewer.app`と`Applications`フォルダへのショートカットを含む`AIImageViewer.dmg`を作成（背景画像等のカスタマイズなしのシンプルな構成）
* `.dmg`本体にも`codesign`で署名し、`.app`とは別に`.dmg`自体も`xcrun notarytool submit`で公証申請・`Accepted`・`xcrun stapler staple`まで完了
* 実際に`open`でマウントして警告なしで開けること、マウント後の`.app`が`spctl`で`accepted`・`source=Notarized Developer ID`となることを確認
* 成果物: `dist/AIImageViewer.dmg`

## dmg配布タスク8: 別ユーザーアカウントでのクリーン環境テスト 完了
* 検証用ユーザーアカウントを作成し、`AIImageViewer.dmg`をiPad経由でAirDropして検証
* AirDrop受信時の確認ダイアログで「Appleによるチェックで悪質なソフトウェアは検出されませんでした」と表示され、公証が正しく機能していることを実機で確認
* 初回起動時の「データベースが見つかりません」ダイアログが正しく表示され、「新しい空のデータベースを作成する」で開始できることを確認
* 元アカウント側の画像データ（142件）は一切見えず、ユーザーごとのデータ分離（Application Support配下への保存設計）が正しく機能していることを確認
* サンプルフォルダの取り込み・一覧表示・編集・設定画面など基本機能の動作も確認

## dmg配布タスク9: サンプルデータの同梱設計と実装 完了
* サンプル画像生成ツール（`ai_image_generator`プロジェクト）で、A1111/NovelAI/ComfyUI形式のメタデータを埋め込んだダミー画像7枚を生成し、`sample_data/`フォルダに同梱（実際のAI生成画像ではないダミー画像のため著作権上の問題なし）
* 「データベースが見つかりません」ダイアログに、「新しい空のデータベースを作成する」を選んだ場合のみ有効になる「サンプルデータを読み込む」チェックボックスを追加
* チェックした状態でOKを押すと、DB作成後に`sample_data/`の画像を`Application Support/AIImageViewer/sample_images/`へコピー（以後は通常の画像と同じく削除・編集可能な実データとして扱う）し、既存のフォルダ取り込み処理でDBへ登録
* `get_resource_dir()`を新規追加し、`python3 main.py`での直接実行時と`.app`（PyInstaller）実行時の両方でサンプル画像の場所を正しく解決できるようにした。`AIImageViewer.spec`の`datas`にも`sample_data`を追加
* 実機で「データベースが見つかりません」状態を再現し、サンプル画像が正しく取り込まれることを確認

## 開発フォルダの整理・sync_deploy.command修正
* `sync_deploy.command`のテスト運用フォルダへのコピー対象に`sample_data/`が漏れていたのを修正（`DatabaseMissingDialog`の「サンプルデータを読み込む」チェックボックスがテスト運用フォルダ側で無効化されてしまう問題を解消）
* `design/design_sheet.md`の`DatabaseMissingDialog`欄（6.1章）に、タスク9で追加した`chk_sample_data`の記載漏れを修正
* 開発フォルダ内の不要ファイルを削除: `build/`（PyInstaller中間生成物）、`__pycache__/`、`.DS_Store`各所、`dist/AIImageViewer.zip`（公証申請用の一時ファイル）、`current_db.txt`（Application Support移行前の名残）

## 追加機能38: 「変更を保存」ボタンの反応をわかりやすく
* 単一画像の編集保存・複数選択での評価一括保存、いずれも成功時にステータスバーへ完了メッセージを表示するようにした（複数選択の一括保存は元々ロック中除外時のみ表示されていたが、通常成功時にも表示するよう統一）
* 「変更を保存」ボタン自体も、押した瞬間に「✓ 保存しました」表示へ一時的に切り替わり、1.2秒後に自動的に元のラベルへ戻るようにした（`_flash_save_button()`を新規追加）。表示中はボタンの幅・アイコンの重複表示（文字とアイコンで二重にチェックマークが出ていた問題）を修正
* ステータスバーのメッセージ表示を、画面下部中央寄せの専用ラベルに変更（`show_status_message()`を新規追加、既存の`statusBar().showMessage()`呼び出しをすべて置き換え）。目立たせる目的のため左寄せから中央寄せへ変更し、表示時間も全体的に延長（3〜4秒→4〜5秒、ファイル未検出通知は8秒→9秒）
* `design/design_sheet.md`に保存フィードバック関連（`_flash_save_button()`・`show_status_message()`）の記載を追加
* dmg配布タスク10（サンプル入り完成版の再ビルド・再配布）完了。最新コード（サンプルデータ機能・保存フィードバック改善等）を反映して再ビルド→再署名→再公証（`.app`・`.dmg`とも`status: Accepted`）→再ステープルまで実施し、`dist/AIImageViewer.dmg`を最新版に更新 <br><br>

## - 2026-08-04
---
### CHANGED

## 追加機能 36: データベース・設定の保存先を Application Support 配下へ移行
* データベース（`image_metadata*.db`）と現在使用中DBのポインタ（`current_db.txt`）の保存先を、アプリのプロジェクトフォルダ内から `~/Library/Application Support/AIImageViewer/` へ変更した（dmg配布に向けた準備）
* 初回起動時、新しい保存先にまだデータベースが無ければ、旧プロジェクトフォルダ内のファイルを自動でコピー移行する（移行はコピーのみ・旧ファイルは削除しないため、問題があればすぐ元の状態に戻せる）
* `database.py`内の全17箇所・`app.py`内の全10箇所にあった`sqlite3.connect(DB_NAME)`（実行時のカレントディレクトリに依存する相対パス接続）を、保存先ディレクトリを明示した絶対パス接続に統一した。今後.app化してもDB接続が壊れない設計になった

## 追加機能 37: データベース未検出時の選択ダイアログ
* 既定の保存先（`~/Library/Application Support/AIImageViewer/`）に、現在使用するはずのデータベースファイルが存在しない場合（初回起動、または利用者がファイルを直接削除した場合）に、これまで自動で新規作成していたのをやめ、選択ポップアップを表示するようにした
* 選択肢: 「新しい空のデータベースを作成する」「同じ保存先フォルダ内の別のデータベースを選ぶ」「外部のバックアップファイルを指定して読み込む」
* 外部ファイルを読み込む場合は、有効なデータベース形式かどうかを検証したうえで、既定の保存先へ新しいスロット名（例: `image_metadata2.db`）としてコピーし、そちらを使用する。コピー後は「新しいデータベースを作成しデータをコピーしました。データベース名は image_metadata2 です。」のように結果を通知する
* アプリ内の「選択中のデータベースをリセット」（テーブルの中身のみ削除、ファイルは残る）はこのダイアログの対象外

## ドキュメント更新: マニュアルへのバックアップ・移行手順の追加
* manual.mdに新章「5. データのバックアップ・移行」を追加。データベースが見つからない場合の選択ダイアログでは「新しい空のデータベースを作成する」（既定選択）を推奨する旨を明記
* データベースの保存場所（`~/Library/Application Support/AIImageViewer/`）、バックアップの取り方、アプリ削除・再インストール時の注意、別環境への移行手順（外部バックアップ読み込み機能を使う方法）を記載
* 既存の「5. 既知の制限事項」は「6. 既知の制限事項」に繰り下げ

## ドキュメント更新: README目次の追加、アプリ内ヘルプの章構成をマニュアルに統一
* README.mdに目次を追加
* アプリ内ヘルプ（`app.py`の`HELP_TEXT`、設定画面「ヘルプを表示」から表示）に目次を追加し、manual.mdと同じ6章構成（1. UI名称と役割／2. 基本的な使い方／3. 設定画面／4. フォルダ画像の同期ルール／5. データのバックアップ・移行／6. 既知の制限事項）に合わせて段落を再編成した
* アプリ内ヘルプは引き続き「GitHubマニュアルの簡易版」という役割・文章量の方針は維持（詳細はマニュアルへの導線を残す）

## dmg配布タスク2: requirements.txtの作成
* `requirements.txt`を新規作成し、実利用環境（`~/Developer/ai_image_viewer`）で動作確認済みのバージョンに固定した: `PySide6==6.10.3`、`Pillow==11.3.0`
* README.mdのインストール手順を`pip install PySide6 Pillow`から`pip install -r requirements.txt`に変更

## dmg配布タスク3: アプリアイコン（.icns）の確定
* 「若葉＋水滴バブル」案を採用し、猫＋蝶案の素材は削除
* 元画像は768×768pxのキャンバスに絵柄が小さく配置されており余白が大きかったため、絵柄部分をトリミングして1024×1024pxへリサイズ
* トリミング後の画像から`AppIcon.icns`（16〜1024pxの標準9サイズを内包）を生成し、`design/app_icon_source/`に保存

## dmg配布タスク4: ビルドツール選定とspecファイル作成
* ビルドツールに`PyInstaller`を採用。実行専用の`requirements.txt`とは分けて、`requirements-build.txt`に`PyInstaller==6.21.0`を記載
* `AIImageViewer.spec`を新規作成。`main.py`をエントリーポイントとし、`design/app_icon_source/AppIcon.icns`をアプリアイコンに指定、`.app`として出力する構成
* 外部リソースファイルを読み込まない設計のため、`datas`（同梱リソース）の指定は無し
* 実際のビルド・動作確認はmacOS環境が必要なため、タスク5でお手元のMacにて実施

## dmg配布タスク5: .appの生成と動作確認 完了
* `pyinstaller AIImageViewer.spec`により`dist/AIImageViewer.app`のビルドに成功
* 実機での起動確認: 新アイコンがDockに表示、既存データ（画像142件・8フォルダ）がApplication Support配下から正しく読み込まれることを確認

## タスク1（DB保存先移行）の見落とし修正
* 「新しいデータベースを作成」機能のdocstring・確認ダイアログ文言が、保存先をApplication Support配下へ移行した後も「このプロジェクトフォルダ内に作成します」という旧文言のままになっていたのを修正
* 設定ダイアログのデータベース欄にあった、旧設計（TCC回避のためプロジェクトフォルダ内に限定）を説明するコメントも、現在の設計に合わせて更新

## README.mdの記載漏れ修正
* 「複数データベースの切り替え」説明文が、DB保存先のApplication Support移行（タスク1）後も「プロジェクトフォルダ内に固定」という旧文言のままだったのを修正

## .gitignoreの見直し（ビルド生成物の除外）
* GitHub上のリポジトリを確認したところ、PyInstallerのビルド生成物（`build/`・`dist/`）とローカル環境依存の状態ファイル（`current_db.txt`）がコミットされていたため、`.gitignore`に追加
* 既にコミット済みのファイルは`.gitignore`だけでは除外されないため、`git rm -r --cached`での対応が別途必要（作業者側で実施）

## 旧DBファイルの削除
* プロジェクトフォルダ直下に残っていた`image_metadata.db`（タスク1でのApplication Support移行前の空の初期ファイル、images件数0）を削除。現在使用中のデータベースはApplication Support配下のもののみ

## 開発ワークフローの自動化: 同期・デプロイランチャー
* `sync_deploy.command`を新規作成。ダブルクリックで、(1)リポジトリ用フォルダへの同期、(2)テスト運用フォルダ（`~/Developer/ai_image_viewer`）へのアプリ部分コピー、を選択して実行できるようにした
* リポジトリ同期は`.gitignore`相当の一時ファイル・生成物を除外して同期（`--delete`なし、git操作自体は手動）
* テスト運用コピーはアプリ4ファイル・`requirements.txt`・ドキュメント一式（`design/`・`dmg_project_docs/`含む）が対象

## dmg配布タスク6: コード署名・公証 完了
* Apple Developer Program個人登録が完了、Developer ID Application証明書も発行済み
* `entitlements.plist`を新規作成（Hardened Runtime対応。PySide6/Qtの動作に必要な最小限の許可のみ付与）し、`AIImageViewer.spec`の`codesign_identity`・`entitlements_file`に反映
* お手元のMacで再ビルド後、`.app`全体への明示的な再署名（`codesign --deep --force --options runtime ...`）と検証（`codesign --verify --deep --strict`）を実施し、`valid on disk`を確認
* 公証用のApp用パスワードを発行し、`xcrun notarytool store-credentials`でキーチェーンに認証情報を保存
* `.app`をZIP化のうえ`xcrun notarytool submit`で公証を申請し、`status: Accepted`で審査通過
* `xcrun stapler staple`でステープル完了。`spctl --assess --verbose`で`accepted`・`source=Notarized Developer ID`を確認し、Gatekeeper警告なしで起動できる状態になったことを確認 <br><br>

## - 2026-08-03(2)
---
### CHANGED

## 追加機能 35: ツールチップ・ヘルプ文言の見直し
* 「フォルダを取り込む」の対応形式表示（ボタンのツールチップ、ヘルプ画面）に「GIF / BMP / TIFF（選択可）」を追記。取り込み画面下部のチェックボックスで既にオン/オフを選べていた仕様と表記を一致させた
* スライドショーのツールチップを「自動で画像を切り替える」→「自動で画像を再生する」に変更
* データベース関連のツールチップを簡潔化: 「新しいデータベースを作成」（改行を挟み2行表示に）、「画像一覧をCSVでエクスポート」、「データベースを初期化」、「この設定をすべてリセット」
* manual.mdの対応形式・「採番リセット」表記を実際のUI表記（GIF/BMP/TIFF選択可・「採番のリセット」）に合わせて修正 <br><br>

## - 2026-08-03
---
### CHANGED

## 追加機能 33: 自動採番ルール保存時の確認ポップアップ
* 設定画面の「自動採番ルールを保存」を押した際、これまでは即座に確定していたが、「新しいルールを保存してもいいですか？(次回以降の新規取り込みの採番から適用されます)」という確認ポップアップ（はい/いいえ）を挟むようにした
* 「いいえ」を選んだ場合は保存処理を行わず、変更前の入力値がフォームに残ったままになる <br><br>

## 追加機能 34: データベース保存フォルダを開くボタンを追加
* 設定画面の「現在使用中: (データベース名)」の右にフォルダアイコンのボタンを追加し、押すとデータベースファイルの保存フォルダをFinder（Windowsはエクスプローラー）で開く（既存の「Finderで表示」と同じ仕組みを利用）
* 実装メモ: 保存先パスをUI側にハードコードせず、`database.get_current_db_path()` という新しい取得関数を経由する形にした。将来的なdmgパッケージ化に伴いデータベースの保存先を `~/Library/Application Support` 等へ移行する際も、この関数の中身を差し替えるだけでよく、呼び出し側（UI）の変更は不要になる <br><br>

## - 2026-07-30
---
### CHANGED

## 追加機能 28: 重複画像の取り込み可否を選べるように
* 「取り込み順序の確認」ウィンドウの最下部に「中身が同じ画像も取り込む」チェックボックスを追加（既定はオフ＝これまで通り取り込まない）
* 「毎回確認する」設定時はダイアログで都度選択。「自動選択する」設定時・同期（🔄）・個別ファイル取り込み時は、前回選んだ内容を記憶して自動適用
* 実装方式: これまでデータベースの構造（file_hashのユニークインデックス）で強制的にブロックしていたが、「時と場合によって許可する」ことができないため、アプリケーション側での判定に変更した。既存のデータベースも自動的に移行される
* 重複を許可して取り込んだ画像は、それぞれ独立したレコードとして登録され、評価・名前・プロンプトの編集も個別に行える（見た目は同じになるため、リスト上では区別がつきにくい点に留意） <br><br>

## 追加機能 29: 取り込み順序に「ファイル名逆順」を追加
* これまで「ファイル名順」「作成日時順（昇順/降順）」「更新日時順（昇順/降順）」はあったが、ファイル名順だけ逆順が無かったため追加
* ファイル名順の判定基準をそのまま反転させた単純な逆順（例: 03, 02, 01の順） <br><br>

## 追加機能 30: 検索対象に実際のファイル名（拡張子含む）を追加
* これまで検索欄は「名前」欄（アプリ内の自由な表示名）・プロンプト・ネガティブプロンプト・その他パラメータのみが対象で、パソコン上の実際のファイル名は対象外だった
* 「名前」欄を変更していても、実際のファイル名の一部や拡張子（`.png`等）で検索できるようにした <br><br>

## 追加機能 31: フォルダ別グループ表示中はグリッド表示ボタンを無効化
* フォルダ別グループ表示はリスト表示でのみ意味を持つため、グループ表示がONの間はグリッド表示への切り替えボタンをグレーアウトするようにした（逆方向＝グリッド表示中はグループ表示ボタンを無効化、は既存機能） <br><br>

## 追加機能 32: フォルダ別グループ表示の並び順を編集できるように
* フォルダの見出し行を右クリックすると「📑 フォルダの並び順を編集...」メニューが表示され、選ぶと専用のダイアログが開く
* ダイアログ内でフォルダ名をドラッグ&ドロップして並び替え、「この順序で保存」で確定。次回起動時も記憶される
* 新しく取り込まれた（並び順未登録の）フォルダは、末尾にアルファベット順で自動的に追加される
* 検討の過程で「設定画面に置く」「並べ替えの選択肢に追加する」など複数の案を比較し、最終的に「フォルダの見出し行を右クリック」という導線に決定。当初は設定画面にもボタンを併設していたが、設定ウィンドウが縦長になる問題があったため、右クリックメニューのみに一本化した（設定画面側のボタンは削除） <br><br>

## 追加機能 33: フォルダ別グループ表示のON/OFF・折りたたみ状態を記憶
* これまで並べ替え条件（種類・昇順/降順）は記憶されていたが、「フォルダ別グループ表示のON/OFF」「どのフォルダを折りたたんでいたか」は毎回リセットされており、ルールが不統一だった
* 両方とも設定として保存し、次回起動時に復元されるようにした <br><br>

## UI微調整
* 設定画面「使用中データベース」のコンボボックスと「このデータベースに切り替える」ボタンの高さを揃えた（32pxに統一） <br><br>

---
### FIXED

## フォルダ別グループ表示の状態を復元すると起動時にクラッシュする不具合
* 具体例: フォルダ別グループ表示がONの状態でアプリを終了し、再起動すると起動しなくなっていた
* 原因: 上記「追加機能33」の実装に伴う副作用。起動直後の画像一覧読み込み処理が、テーマ色（`self.theme_colors`）を決定する処理より先に実行される順序になっていた。これまでは起動直後は必ずグループ表示OFFから始まっていたため問題が表面化していなかったが、ON状態を復元するようになったことで、テーマ色が未設定のままフォルダの見出し行を色付けしようとしてクラッシュするようになっていた
* 対応: テーマを適用する処理を、画像一覧の初回読み込みより前に実行する順序へ修正 <br><br>

---
### CHANGED

## デザインの見直し: 絵文字からSVGアイコンへの統一
* リスト項目の「編集ロック」表示・ツールチップは、サムネイル表示が既にアイコン欄を使用しているため絵文字（🔒）のまま維持
* 右クリックメニュー（Finderで表示・ファイルをコピー・ファイルパスをコピー・編集をロック/ロック解除・フォルダの並び順を編集）は、いずれも絵文字から実際のアイコン表示に変更。ロック関連は新規に用意した`lock`/`unlock`アイコンを使用、それ以外は既存のアイコン（`folder`・`content_copy`・`sort`）を流用
* ヘルプ本文中の絵文字（🔄・📁）による案内は、実際のボタン名を使った文章に修正（該当ボタン自体は既存のアイコンを使用済みだったため、文章側の表記のみの対応）
* 一括評価保存メッセージの「⭐」を、リストの評価表示と同じ「★」に統一（表記ゆれの解消） <br><br>

## 危険操作ボタンの配色を縁取り型に変更
* 「選択した画像をリストから削除」「採番リセット」「データベースを初期化」「この設定をすべてリセット」の4ボタンについて、背景を塗りつぶす配色から、**通常のボタンと同じ背景色＋テラコッタ色の枠線・文字色**による縁取り型のデザインに変更（印象を和らげるため）
* 配色変更に伴い、ダークモード用（#e08972）・ライトモード用（#8a3427）に、それぞれ十分なコントラスト比（WCAG AA基準の4.5以上）を確保した専用の色を新たに設定
* アイコンの色もテーマの切り替えに追従して自動的に再生成されるよう対応 <br><br>

---

## - 2026-07-29
---
### FIXED

## メインウィンドウを閉じても全画面プレビュー・読書モードが残る不具合
* 具体例: プレビューが全画面表示されている状態で、複数の仮想デスクトップを切り替えて操作すると、アプリを終了してもプレビュー画面が別のデスクトップに残り続けていた
* 原因: 全画面プレビュー・読書モードは、いずれも独立したトップレベルウィンドウとして作られており、メインウィンドウを閉じても自動的には閉じない構造だった
* 対応: メインウィンドウに`closeEvent`を追加し、閉じる際に開いている全画面プレビュー・読書モードがあれば一緒に閉じるようにした <br><br>

---
### CHANGED

## 危険操作ボタン4種の配色統一・ソフト化
* 「選択した画像をリストから削除」「採番リセット」「データベースを初期化」「この設定をすべてリセット」の配色ルールを点検したところ、「この設定をすべてリセット」だけ赤くなっていない不統一が見つかった
* 4つとも統一した上で、従来の強い赤（#ce352c）から、落ち着いたテラコッタ系の色（#a8483d）に変更。スライドショー再生中ボタンの赤（同じ`danger_bg`を参照していた）とは独立した専用の色（`caution_bg`）に分離し、影響が及ばないようにした
* あわせて、配色変更に気づいた「データベースを初期化」ボタンのアイコン色（背景に対して視認性の低い灰色のままだった）も白に修正 <br><br>

## ボタン名・ツールチップの全体最適化
* アプリ内の全37ボタンを棚卸しし、ツールチップが未設定だった14個のうち12個に説明文を追加（「Escキーでも可」等、ラベル自体で説明済みの2個は見送り）
* ボタン名を3件変更: 「切り替える」→「このデータベースに切り替える」／「▶ スライド」→「▶ スライドショー」（状態変化時の表記と統一）／「コピーを保存」→「別名で保存」 <br><br>

## 設定ダイアログのさらなる調整
* 生成パラメータの表示項目チェックボックス（Steps等6項目）を、1段の横並びから2段×3列のグリッド表示に変更
* ダイアログの最小幅を600px→480px（20%減）に縮小
* ダイアログの既定の縦の長さを、「画面の85%」から「アプリのメインウィンドウの現在の高さに統一」する方式に変更（項目が収まりきらない場合はスクロールで対応する方針は変更なし）
* 読書モード起動ボタンのアイコンを、より本らしさを強調したデザインに差し替え <br><br>

## 追加機能 27: 読書モードの中央詰め表示
* 見開き表示の左右のページを、実際の本や電子書籍のように画面中央でぴったり突き合わせて表示するオプションを追加
* 背景切替ボタンの右隣に専用のトグルボタンを配置。オン時は左ページを右揃え、右ページを左揃えにすることで、あぶれた余白を画面の外側だけに追いやる仕組み
* 切り替えるたびに設定として保存され、次回もこのモードを開いた時に記憶される。設定画面の読書モードグループにも既定値のチェックボックスを追加 <br><br>

---

## - 2026-07-28
---
### CHANGED

## 追加機能 25: 読書モード（見開き表示）
* 選択した画像と同じフォルダ内の画像を、本のように2枚並べて全画面表示する新機能を追加
* 「フォルダ別グループ表示」が有効な時のみ利用可能（ボタンはそれ以外の時はグレーアウト）
* 開き方向はパターンA（左開き・左から右へ読む）／パターンB（右開き・右から左へ読む）を切り替え可能。既定値は設定画面で選べ、見開き表示中にその場で切り替えた内容はセッション限りの一時的な変更として扱う（既定値は上書きしない）
* ページの並び順は、アプリの現在の並べ替え設定に関わらず、常に「名前順」で固定
* 操作方法: 下部のスライドバー（見開き単位でスナップ）、最初/最後の見開きへのジャンプボタン、矢印キー、画面端（幅の15%）クリックに対応。矢印キー・端クリックは、画面上の左右ではなく「その時点の読む方向」に従って動作する
* 画面右上に、現在の開き方向を示す半透明ラベルを常時表示
* 閲覧専用（このモード中の編集は不可）
* 実装方式: 既存の全画面プレビュー（`FullscreenPreviewWindow`）とは独立した専用クラス（`ReadingModeWindow`）として実装し、影響範囲を分離 <br><br>

## 追加機能 26: 読書モードの背景テーマ（ダーク/ライト）切り替え
* 読書モードの背景・コントロールバー・各ボタン・パターン表示ラベルの配色を、ダーク/ライトでまとめて切り替えられるボタンを追加（スライドバー左端に配置）
* アプリ本体のテーマ設定とは独立した専用の設定として保存し、次回も記憶する <br><br>

## 設定ダイアログのUI最適化
* 設定項目が増えて縦に長くなり、画面下端（Dock等）と重なる問題に対応
* 外観モード／フォルダ取り込み時の順序／読書モードの開き方向／命名規則の保存・採番リセットボタン／データベースの初期化・CSVエクスポートボタンを、それぞれ横並びに変更
* 設定ダイアログ全体をスクロールエリアに収容し、ウィンドウの高さに画面の85%を上限として設定。既定でこの上限いっぱいの高さで開くため、通常時はスクロールバーが表示されない
* データベースグループの並び順を「現在使用中の表示・切り替え → 新しいデータベースを作成 → CSVエクスポート → データベースを初期化」に変更
* 「データベースを初期化」「画像一覧をCSVでエクスポート」「新しいデータベースを作成」の3ボタンにツールチップを追加 <br><br>

---

## - 2026-07-26
---
### 試みたが撤回した実装（記録として残す）

## OSユーザーごとのデータベース自動分離 → 撤回
* 目的: 同じMacを複数のOSユーザーアカウント（例: ユーザーA、ユーザーBなど）で共有している環境で、アプリ本体は共有しつつ、データベースだけはユーザーごとに自動分離したい、という要望への対応
* 試みた方式: `~/Library/Application Support/AIImageViewer/` 配下にユーザーごとのデータベースを保存する方式、およびその発展として、Macの写真アプリのような複数ライブラリの管理・切り替え機能（`LibraryManagerDialog`）を実装
* 発生した問題: Terminalからの起動では問題なく動作したが、`.app`（Finderからのダブルクリック）経由で起動すると `sqlite3.DatabaseError: authorization denied` で起動不能になった。原因は、対象ファイルに付与されていた `com.apple.provenance` という拡張属性により、署名されていない `.app` からのアクセスがmacOSのセキュリティ機能（TCC）によって拒否されていたためと判明
* 対応として、アクセス不可時にプロジェクトフォルダ内へ自動フォールバックする保険的な実装も追加したが、これは「起動不能を防ぐ」だけで、当初の目的（ユーザーごとの自動分離）自体は`.app`起動時に達成できないままだった
* 最終判断: `task0726 db_per_user_instruction.md` 以降の変更（上記のユーザー別分離・ライブラリ管理機能一式）をすべて撤回し、変更前の状態（単純な相対パス指定）に戻した
* 教訓: `~/Library`配下など、プロジェクトフォルダの外にある保存先は、`.app`起動時にmacOSのセキュリティ機能の影響を受けるリスクがある。次の「複数データベースの切り替え機能」では、この教訓を踏まえてプロジェクトフォルダ内に保存先を限定する設計に変更した <br><br>

---
### CHANGED

## 追加機能 24: 複数データベースの作成・切り替え（プロジェクトフォルダ内限定）
* 上記の教訓を踏まえ、`~/Library`等の外部パスには一切アクセスせず、アプリのプロジェクトフォルダ内だけで完結する設計で再実装
* プロジェクトフォルダ内に、`image_metadata.db`・`image_metadata2.db`のように複数のデータベースを作成できるように
* 使用中のデータベースは、プロジェクトフォルダ内の小さなポインタファイル（`current_db.txt`）に記録し、起動時に参照する。ファイルが無い場合はこれまで通り `image_metadata.db` を使うため、既存ユーザーの挙動は変わらない
* 設定画面の「データベース」グループに、一覧表示・切り替え・新規作成のUIを追加。切り替え時はアプリが自動的に再起動する
* 「新しいデータベースを作成」は、作成前に確認ポップアップを表示し、「いいえ」の場合はファイルを作成しない。作成後は自動で切り替えたりせず、一覧から選んで「切り替える」ボタンを押す必要がある旨を案内するのみに留める（誤操作防止） <br><br>

## 設定ダイアログの調整
* 「表示項目」（Steps/Sampler等6項目のチェックボックス）を縦並びから横並びに変更し、ダイアログの最小幅を拡大
* 設定ダイアログの「閉じる」ボタンを廃止（macOSウィンドウ標準の閉じるボタンと操作が重複するため） <br><br>

---
### 除外設定
* `.gitignore` に `current_db.txt`（使用中データベースを記録するポインタファイル）を追加することを推奨。`image_metadata.db` の指定も、複数データベースに対応するため `image_metadata*.db` に更新することを推奨 <br><br>

---




## - 2026-07-25
---
### CHANGED

## 追加機能 19: 連番命名規則のオン/オフ切り替え
* 設定画面の「新規取り込み画像の連番命名規則」の一番上に、オン/オフのチェックボックスを追加
* オン（既定）: これまで通り連番（例: CG_00001）を付与
* オフ: 連番を付与せず、実際のファイル名（拡張子を除く）をそのまま「名前」として取り込む <br><br>

## 追加機能 20: 画像一覧のCSVエクスポート機能
* 設定ダイアログの「データベース」グループに「画像一覧をCSVでエクスポート」ボタンを追加
* エクスポート項目は「基本情報のみ」「基本情報＋プロンプト等のメタ情報」から選択可能
* 文字コードはUTF-8 with BOMとし、WindowsのExcelで開いても文字化けしないようにした <br><br>

## 追加機能 21: 「名前」フォームのツールチップ
* 「実際のファイル名とは別の、アプリ内だけの名前です。自由に変更・重複可能なので、検索用のタグのような使い方もできます（初期値は自動採番）。」を表示するように <br><br>

## 追加機能 22: ヘルプダイアログの改善
* `QMessageBox`ベースの実装から専用のダイアログに変更し、不要なアイコンを非表示化
* ダイアログサイズを拡大し、スクロール表示に対応
* GitHubの詳細マニュアル（manual.md）へのリンクを追加 <br><br>

## 追加機能 23: UIボタンの配色見直し
* 「採番リセット」ボタンを、既存の危険操作（削除・データベース初期化）と同じ配色（赤背景・白文字）に統一
* 「画像一覧をCSVでエクスポート」ボタンに新しい配色（緑）を追加。ライト/ダーク両モードでWCAG基準のコントラスト比を確認済み
* 「変更を保存」ボタンは、他のアクセントカラー使用箇所との統一性を優先し、今回は配色を変更せず据え置き <br><br>

## ドキュメント整備
* `manual.md`（GitHub向け詳細マニュアル）を、実際のアプリ仕様に基づいて大幅に拡充（設定画面・対応メタデータ形式・既知の制限事項などを新設）
* UIの配色とカーソルオーバー時の表示文言を一覧化した資料を新規作成 <br><br>

---
### FIXED

## フォルダ別グループ表示を折りたたんだ状態で画像送りするとアプリがフリーズする不具合
* 具体例: フォルダを折りたたんだ状態で、直前に選択していた画像をプレビュー表示中に、前/次の画像へ移動するボタンを押すとアプリ全体がフリーズしていた
* 原因: 「次に表示できる画像を探す」ループの終了条件が、探索中の行番号が開始時点の選択行と一致したら止まる、という作りだった。折りたたんだフォルダの画像が選択中だった場合、選択行がリストから消えて「未選択（-1）」になり、探索中の行番号（0以上に補正される）とは理論上決して一致しないため、無限ループになっていた
* 対応: 折りたたみ中のフォルダの画像も、リストからは取り除かず非表示にするだけの方式に変更（検索の絞り込みと同じ仕組みを流用）し、折りたたんだままでも画像送りが正しく機能するようにした。あわせて、探索ループ自体も「リストの件数分だけ探したら必ず終了する」構造に修正し、同種の状況で再発しないようにした <br><br>

---
## - 2026-07-23
---
### 提案・未実装のアイデア（IDEAS / PROPOSED）
今回の会話で検討したもので、まだ実装していないアイデアを記録しておく。実装はトークン上限の関係で後日を予定。

## 画像一覧のCSVエクスポート機能
* 標準の`csv`モジュールで実装（新規の依存パッケージ不要）、UTF-8 with BOMでExcel文字化けを回避
* 未確定の点: エクスポート項目の範囲（基本情報のみ/プロンプト系含む/全項目）、対象範囲（常に全画像 or 現在の検索フィルタを反映）、ボタンの設置場所（設定ダイアログ内 or リスト表示エリア） <br><br>

## 「名前」フォームのツールチップ追加
* 文言（確定済み）: 「実際のファイル名とは別の、アプリ内だけの名前です。自由に変更・重複可能なので、検索用のタグのような使い方もできます（初期値は自動採番）。」 <br><br>

## 対応画像形式の分かりやすい案内
* 「フォルダを取り込む」ボタンのツールチップに対応形式（PNG/JPG/JPEG/WEBP）を明記
* ヘルプ画面にも同様の記載を追加 <br><br>

---
## - 2026-07-22
---
### CHANGED

## 追加機能 17: フォルダ別グループ表示の折りたたみ機能
* フォルダ別グループ表示中、見出し行（フォルダアイコン＋フォルダ名）をクリックすると、そのフォルダの画像一覧を折りたたみ/展開できるように
* 折りたたみ中は見出しの先頭に「▶」、展開中は「▼」を表示して状態を判別
* フォルダごとに独立して折りたたみ状態を保持（検索・並び替え・画像の選び直しをしても維持）
* 実装方式について: 当初、見出し行に折りたたみ用のボタンを埋め込む方式（`setItemWidget`）を試みたが、
  行の幅がリストの表示幅に正しく追従せず、繰り返し不具合が発生したため撤回。
  代わりに、見出し行全体をクリック対象とする、より安定した実装方式を採用した <br><br>

## 追加機能 18: ComfyUI形式のメタデータ対応
* 画像に埋め込まれた `workflow`（ノード構成全体）・`prompt`（実行用APIフォーマット）のJSONデータを検出し、「その他パラメータ / メタデータ」欄に保存するように
* ワークフロー構成はユーザーごとに大きく異なり、プロンプト・LoRA・バッチサイズ等を確実に自動判別する方法がないため、あえて構造化した抜き出しは行わず、生のJSONをそのまま保持する方針とした
* プロンプト欄には、ComfyUI形式である旨と、その他パラメータ欄を確認するよう促すメッセージを表示
* 従来のAutomatic1111 / Forge / NovelAI形式には影響がないことを確認済み <br><br>

---
### FIXED

## 新規データベース作成時の設定初期値シード処理の欠落
* 具体例: 生成パラメータの個別表示など一部の設定項目について、新規データベースでは既定値が反映されず、意図しない挙動になっていた
* 原因: `init_db()` 内で、未設定の項目にのみ既定値を書き込む移行処理が、何らかの経緯で失われていた
* 対応: 移行処理を復元。既存ユーザーが変更済みの値は上書きしないことをあわせて確認 <br><br>

## 生成パラメータ個別表示のデフォルト値の巻き戻り
* 具体例: 「Steps/Sampler等の個別表示」の初期値が、以前「非表示」に変更していたはずが「表示」に戻ってしまっていた
* 対応: 既定値を「非表示（0）」に修正 <br><br>

---
## - 2026-07-21
---
### CHANGED

## 追加機能 14: メタデータ編集欄の表示/非表示切替
* 「使用モデル」「プロンプト」「ネガティブプロンプト」「その他パラメータ」の各見出し行に、編集フォームの表示/非表示を切り替えるボタンを追加（表示/非表示切替 → コピー → クリア の順、間隔12px）
* 非表示にした分の高さを、残っている表示中のフォームへ均等に自動配分
* 表示/非表示の状態は画像を選び直しても維持される <br><br>

## 追加機能 15: 確認ポップアップのアイコン統一
* 「はい/いいえ」の選択を伴う確認ダイアログ（削除確認・各種リセット確認）のアイコンを、標準アイコンから専用SVGアイコンに統一
* 共通ヘルパー関数 `show_confirm_dialog()` を新設し、今後追加する確認ダイアログもここ1箇所で統一管理できる設計に <br><br>

## 追加機能 16: 区切り線の追加
* 「プレビューエリア」と保存/コピー保存/スライドショー/再生速度ボタン行の間、および「使用モデル」「プロンプト」「ネガティブプロンプト」「その他パラメータ」各見出しの上に、画像リストの罫線と同様の薄い横線を追加
* ダークモードでは背景に同化しないよう、専用の明るめの色を使用 <br><br>

## UIデザインシートの作成
* ボタン・フォーム等のUI要素の表示名・変数名・objectNameを一覧化した `design_sheet.md` を新規作成
* 今後のUI調整依頼時に、該当箇所を素早く特定できるようにするための資料 <br><br>

---
### FIXED

## フォルダ別グループ表示の見出しがライトモードで見えなくなる不具合
* 具体例: フォルダ別グループ表示の見出し文字が、ライトモードでは背景と同化してほぼ見えなくなっていた（ダークモードは正常）
* 原因: `QListWidget` 本体に設定していたQSSの文字色指定が、個々の項目に対する文字色指定より優先されており、見出しの背景色（両テーマ共通で固定の青）とのコントラストがライトモードでのみ不足していた
* 対応: 見出しの背景色を、実際に描画される文字色と確実にコントラストが取れるテーマ専用色に変更 <br><br>

## 空リスト/ドラッグ中プレースホルダーの背景色
* 具体例: 「画像を取り込んでください」等の案内表示の背景が、周囲と同化して見えていた
* 対応: プロンプト等の入力欄と同じ背景色（ライト: 白、ダーク: 黒に近い色）を適用 <br><br>

## ドラッグ&ドロップでの画像取り込みを一時的に無効化
* 具体例: 外部（Finder等）からのドラッグ&ドロップ取り込みの動作が不安定だった
* 対応: `ImageListWidget` の `setAcceptDrops` を無効化し、取り込みを「フォルダを取り込む」「画像を取り込む」ボタンに一本化。関連コードは残してあり、将来的な再有効化は容易 <br><br>

## 標準サイズのプレビュー画像が画像送りのたびに肥大化する不具合
* 具体例: プレビューを「標準」サイズで表示中、画像を連続で送ると、プレビュー画像が徐々に大きくなり、ある程度で頭打ちになっていた（コンパクト・全画面表示では発生しない）
* 原因: プレビューラベルの `QSizePolicy.Expanding` が、ラベル自身のsizeHint（表示中の画像の内容に応じて変動）をレイアウト計算に含めてしまい、画像送りのたびにサイズがわずかにずれて蓄積するフィードバックループが発生していた
* 対応: `QSizePolicy.Ignored` に変更し、sizeHintをレイアウト計算から除外することでループを解消 <br><br>

## メタデータ編集欄コピー機能の実装中に混入したコード構造の破損
* 具体例: コピーボタン押下時の完了フィードバック処理（✓マーク表示）が、別のメソッドの末尾に誤って迷い込んでいた
* 対応: 該当コードを正しいメソッド内に戻し、メソッド構造に重複・破損がないことを静的解析で確認 <br><br>

---
### 余白の微調整
* 「名前」アイコン＋ラベルと名前入力欄の間隔を約12px縮小
* 編集エリアとウィンドウ右端の余白を約12px拡大
* プレビューエリアと保存/コピー保存/スライドショー/再生速度ボタン行との間隔を約12px拡大
* リスト表示エリアと編集エリアの間、および編集エリアとウィンドウ右端の間に、それぞれ30pxの余白を新設 <br><br>

---
### 提案・未実装のアイデア（IDEAS / PROPOSED）更新
* 「アイコンのテーマ別配色ルールの拡充」は、アクセントカラー背景に加えて確認ポップアップのアイコンにも対応したため、部分的に実装済みとなった
* 残りの項目（採番状況の一覧画面、取り込み前の名前プレビュー、設定のエクスポート/インポート）は引き続き未着手 <br><br>

---
## - 2026-07-20
---
### CHANGED

## 追加機能 9: アイコンデザインの本格導入（Material Symbols）
* ユーザー提供の実際のGoogle Material Symbols（SVG）を組み込み、絵文字アイコンを順次置き換え
* 対象: フォルダ/ファイル取り込み・変更を保存・コピーを保存・スライド・プレビューサイズ（標準/コンパクト/全画面/終了）・グリッドサイズ（小/中/大）・設定（歯車）・同期・フォルダ別グループ表示・並べ替え方向（昇順/降順）・検索欄・ヘルプ・リリースノート・データベース初期化・設定リセット・閉じる、など計40種類以上
* 見出し（使用モデル・プロンプト・ネガティブプロンプト・その他パラメータ）にもアイコンを追加し、対応するアイコンがある場合は【】表記を廃止
* 「ファイル名」「名前」ラベルもアイコン＋テキスト表記に変更し、区切りの「:」を廃止
* 並べ替えのドロップダウン各項目（名前・作成日・編集日・取り込み日時・評価・ファイルサイズ）にもアイコンを設定
* アクセントカラー（青）背景のボタン上のアイコンは、視認性のため白色に自動切替（プレビュー/グリッドサイズの選択中、フォルダ別グループ表示ON、複数選択時の保存ボタンなど） <br><br>

## 追加機能 10: 生成パラメータの個別表示
* Steps・Sampler・Scheduler・CFG scale・Seed・Size を、使用モデル欄の下に自動抽出・読み取り専用で表示
* 設定画面に「表示項目」グループを新設し、項目ごとにチェックボックスで表示/非表示を切替可能
* 文字サイズを見出しと同程度に拡大し、左側（画像リストのスクロールバー側）に30px以上の余白を確保 <br><br>

## 追加機能 11: 命名規則のアペンド欄、組み合わせ別の採番、採番リセット
* 命名規則に「アペンド（末尾に追加する任意の文字列）」欄を追加
* プレフィックス＋アペンドの組み合わせごとに採番カウンタを独立させ、組み合わせを切り替えると1から採番し直し、元の組み合わせに戻すと続きから再開
* 設定画面に「採番リセット」ボタンを追加。過去に同名の画像が存在していても考慮せずリセットする旨を実行前に警告表示 <br><br>

## 追加機能 12: 設定のリセット・データベースの初期化
* 「この設定をすべてリセット」ボタンを追加。命名規則・表示項目・テーマ設定を初期値に戻す（連番カウンタ自体は対象外）
* 「データベースを初期化」ボタンを追加。画像・記憶フォルダ・除外リストを削除（実ファイルとアプリ設定は保持）。実行前に削除件数を明示して確認 <br><br>

## 追加機能 13: デザイントークンの導入
* 余白（4/8/12/16/24px）・フォントサイズ（見出し/本文/補足の3段階）・アイコンサイズ（3段階）を、コード内の共通定数として整理
* 既存の余白・文字サイズをこれらの定数に統一（一部、意図的に対象外とした箇所はコメントで明記） <br><br>

---
### FIXED

## 設定メニュー内、連番桁数スピンボックスの視認性（ダークモード）
* 具体例: 「オプションメニュー→連番の命名規則→桁数」のスピンボックスが、背景色（白）と文字色（白）が重なっており、ダークモード時に数字が見えづらかった
* 対応: `QSpinBox` にも他の入力欄と同じテーマ対応スタイルを適用 <br><br>

## スライドショーボタンのアイコン
* 具体例: スライドショー開始/停止ボタンに追加したアイコンが視認性の面で望ましくなかった
* 対応: アイコンを削除し、テキスト（▶/■）のみの表示に戻した <br><br>

---
### 提案・未実装のアイデア（IDEAS / PROPOSED）
今後の参考として、これまでの会話の中で提案したものの、まだ実装していないアイデアを記録しておく。

## UI統一性のさらなる改善
* アイコン運用ルールの明文化（どんな時にアイコン化するかの基準）
* アイコンのテーマ別配色ルールの拡充（現状はアクセントカラー背景の白化のみ対応）
* アイコン付きボタンの最小幅・余白ルールの体系化 <br><br>

## 機能面の改善案
* 命名規則ごとの採番状況を一覧表示できる画面
* 画像取り込み実行前に、付与される予定の名前をプレビュー表示する機能
* 設定一式（命名規則・表示項目・テーマ等）をJSON等でエクスポート/インポートする機能 <br><br>

---
## - 2026-07-19
---
### CHANGED

## 追加機能 1: 削除した画像を同期対象から除外
* 新しいテーブル `excluded_paths` を追加し、削除したファイルのパスを記録
* 「同期」実行時は、このリストに載っているパスをスキップ
* 「フォルダを取り込む」「画像ファイルを取り込む」で明示的に再度選ばれたファイルは、リストから解除して通常通り取り込む <br><br>

## 追加機能 2: 画像の編集ロック機能
* 右クリックメニューから画像単位で編集をロック/解除できるように
* ロック中は「削除」を禁止
* ロック中は編集欄（ファイル名・DB内表示名・評価・プロンプト等）をグレーアウトして操作不可に
* リスト・グリッド表示にロックアイコン（🔒）を表示し、ひと目で判別可能に <br><br>

## 追加機能 3: メタデータ欄にクリアボタンを追加
* 使用モデル・プロンプト・ネガティブプロンプト・その他メタデータの各コピーアイコン左に🗑ボタンを配置
* 「変更を保存」を押すまでは一時的な変更のままで、保存した時点で確定 <br><br>

## 追加機能 4: サムネイルサイズ調整（小/中/大）
* グリッド表示時に、タイル幅・アイコンサイズを3段階から選べるように
* 「大」サイズ選択時のみ、タイルに余裕があるため場所・日時も表示 <br><br>

## 追加機能 5: フォルダごとのグループ表示
* リスト表示時、フォルダ別に見出し行を挿入してグループ表示できるように（日付グループ等とは排他）
* 見出し行はフォルダアイコン＋フォルダ名で表示、選択・ドラッグ・削除の対象外
* グリッド表示中はこの機能を無効化 <br><br>

## 追加機能 6: 画像リストへのドラッグ&ドロップ取り込み
* Finder等の外部から、画像ファイル・フォルダを画像リストへ直接ドラッグ&ドロップして取り込み可能に
* フォルダはフォルダ単位で取り込み・同期対象として記憶、個別ファイルはファイル単位で取り込み <br><br>

## 追加機能 7: 「DB内表示名」→「名前」に変更、新規取り込み時のデフォルト値を連番に
* フィールド名を「DB内表示名」から「名前」に変更
* 新規取り込み画像の「名前」初期値を、実ファイル名ではなく連番（例: CG_001）に変更
* 連番は画像を削除しても再利用しない（欠番のまま進む） <br><br>

## 追加機能 8: 歯車アイコン+設定メニュー
* 連番命名規則（プレフィックス・桁数）の編集、リアルタイムプレビュー
* ダーク/ライトモードの手動切り替え（自動/ダーク固定/ライト固定）
* ヘルプ（静的テキスト表示）
* リリースノート（GitHubリポジトリへのリンク） <br><br>

---
### FIXED

## 「変更を保存」ボタンの表示が戻らない不具合
* 具体例: リスト項目をすべて削除したあと、「変更を保存」ボタンが「選択した◯◯件の評価を一括保存」の表示のまま戻らなかった
* 原因: `delete_image_from_db` で、複数選択時に変わった保存ボタンのテキスト・状態(`objectName`)をリセットする処理が抜けていた
* 対応: 単一選択時と同じリセット処理を削除後にも追加し、再起動なしで自動的に元の表示へ戻るよう修正 <br><br>

## f-string構文エラーによる起動不能（Python 3.9環境）
* 具体例: ロック機能追加時に混入したコードが原因で、Python 3.9環境でアプリ自体が起動できなくなっていた
* 原因: f-stringの`{}`内にバックスラッシュ(`\n`)を含む記法を使用しており、Python 3.11以前では構文エラーになる（3.12から解禁）
* 対応: 該当箇所を、バックスラッシュを`{}`の外に出す書き方に修正。同様のパターンが他にないか全ファイルを検索し確認 <br><br>

## フォルダグループ見出しのアイコン表示
* 具体例: フォルダ別グループ表示の見出しが、絵文字「📁」をテキストに埋め込む形になっていた
* 対応: OS標準のフォルダアイコンを、画像サムネイルと同様にアイコン領域へ表示する方式に変更 <br><br>

## 設定アイコンの位置・サイズ
* 具体例: 設定（⚙）アイコンが検索欄の隣にあり分かりにくく、アイコンも小さかった
* 対応: 画面右上（「名前」フィールドの右）に移動し、アイコンサイズも拡大 <br><br>

---
### PLANNED（今後対応予定）

（現時点で未着手の項目はありません）

---
