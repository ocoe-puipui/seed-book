# Seed Book (AI image browser) - ユーザーマニュアル

**Language:** 日本語 | [English](./manual.md)

## 目次
1. [アプリのUI名称と役割](#1-アプリのui名称と役割)
2. [基本的な使い方](#2-基本的な使い方)
3. [アルバム（仮想フォルダ）機能](#3-アルバム仮想フォルダ機能)
4. [設定画面](#4-設定画面)
5. [フォルダ画像の同期ルール](#5-フォルダ画像の同期ルール)
6. [データのバックアップ・移行](#6-データのバックアップ移行)
7. [既知の制限事項](#7-既知の制限事項)

---

## 1. アプリのUI名称と役割

<img width="1311" height="1071" alt="UI1" src="./design/screenshots/ui_main_light.png" />


本アプリは、左側の「リスト表示エリア」と右側の「編集エリア」の2ペイン、および「設定」で構成されています。

### 1.1 リスト表示エリア（左側）
* **検索・設定行:** 検索欄（任意のキーワードで画像を絞り込み）、設定ボタン（歯車、各種設定ダイアログを開く）。
* **取り込み・同期行:** フォルダを取り込む（指定フォルダ内の画像をまとめて取り込み）、画像を取り込む（指定した画像ファイルを取り込み）、同期ボタン（🔄、フォルダの最新状態と同期）。
* **フォルダ/アルバム切替行:** 通常の画像リスト表示とアルバム表示を切り替えます（[3. アルバム（仮想フォルダ）機能](#3-アルバム仮想フォルダ機能)参照）。アルバム表示中は「アルバムを作成する」ボタンも表示されます。
* **並べ替え・表示切替行:** 並べ替え/昇順・降順切替（「ファイル名順」と「名前順」は別の選択肢）、リスト/グリッド表示切替、フォルダ別グループ表示切替。
* **グリッドサイズ選択行**（グリッド表示中のみ）: サムネイルサイズを「小」「中」「大」から選択します。
* **リスト下部:** ライブラリ情報（取り込み済みフォルダ数・画像数と最終同期日時、未同期なら「未同期」）、削除ボタン（選択画像をリストから削除）。

### 1.2 編集エリア（右側）
* **ファイル名・名前行:** 画像のファイル名や表示名を編集します。
* **プレビューエリア:** プレビューサイズ（非表示 / 標準 / コンパクト / 全画面。「非表示」でメタデータ編集エリアを広く使える）、星評価（★）の付与、前後の画像へのナビゲーション。
* **保存・スライドショー行:** 変更を保存 / 別名で保存（別ファイルとして保存）、スライドショー開始/停止（自動で切り替え、速度も調整可能）。
* **メモ欄:** 編集エリア最上部にある画像ごとの自由記入欄。プロンプト等のメタデータには一切影響せず、アプリ内だけで使う情報で、検索対象にもなります。
* **メタデータ編集欄:** メモ欄の下に「使用モデル / チェックポイント」「プロンプト」「ネガティブプロンプト」「その他パラメータ / メタデータ / EXIF情報」の順で表示。各欄（メモ含む）の見出し行に「表示/非表示」「コピー」「クリア」ボタンがあります。

---

## 2. 基本的な使い方

### 2.1 画像の取り込み

**対応形式:** PNG / JPG / JPEG / WEBP（既定でオン）、GIF / BMP / TIFF（取り込み画面下部のチェックボックスで有効化、既定オフ）。左端の表示/非表示ボタンで欄全体を畳み、リスト表示エリアを広げられます。

* **フォルダを取り込む:** フォルダ直下の画像がまとめて取り込まれます（サブフォルダは対象外）。取り込み順序はファイル名順・逆順・作成日時順・更新日時順（各昇順/降順）から選択。「中身が同じ画像も取り込む」チェック（既定オフ）で内容が完全一致する画像を別レコードとして取り込むか選べます。確認画面は設定画面で「毎回確認する」「前回と同じ設定を自動で使う」に切替可能です。
* **画像を取り込む:** ファイル選択ダイアログから個別に複数選択して取り込みます。フォルダの一部だけ取り込みたい場合に便利です。
* **同期（🔄）:** 取り込み済みフォルダを再スキャンし、新規追加画像を取り込みます。詳細は[5](#5-フォルダ画像の同期ルール)参照。

「中身が同じ画像も取り込む」で取り込んだ画像はそれぞれ独立した別画像として登録され、見た目は同じでも評価・名前・プロンプトの編集は個別管理されます。

### 2.2 対応しているメタデータの種類

画像に埋め込まれた生成AI情報（プロンプト等）は、ツールの種類によって自動判別されます。

| ツール | 対応状況 |
|---|---|
| Stable Diffusion WebUI（Automatic1111 / Forge） | プロンプト・ネガティブプロンプト・その他パラメータを自動分離表示 |
| NovelAI | 同上 |
| ComfyUI | ワークフロー構成が多岐なため自動抽出せず、全体（LoRA・バッチサイズ等含む）を「その他パラメータ」欄にそのまま保存 |

Steps・Sampler・Scheduler・CFG scale・Seed・Sizeの6項目は「使用モデル」欄下に個別表示できます（設定画面で項目ごとに切替可、既定は非表示）。

**カメラ写真のEXIF情報について:** AI生成パラメータがなく、JPEG/TIFF/WEBP等EXIF対応形式の場合、メーカー・機種・撮影日時・露出時間・F値・ISO感度・焦点距離・GPS座標等を自動で読み取り「その他パラメータ」欄に整形表示します（プロンプト欄には案内文）。PNG・GIF・BMPは非対応です。

### 2.3 画像の閲覧・整理

* **プレビュー:** 画像選択で右側に表示。「非表示」「標準」「コンパクト」「全画面」の4モードを切替可能で、全画面中も画像送り・スライドショー・速度調整はそのまま行えます。Finder等へD&Dで取り出せます。クリックしてフォーカスすると、以後は左右キーでも画像送りできます（他の入力欄から誤ってフォーカスが移らないよう、クリック後のみ有効）。
* **星評価:** ★にマウスを乗せるとプレビュー表示、クリックで0〜5段階を設定。同じ星を再クリックで解除。
* **検索:** 「名前」欄・実ファイル名（拡張子含む）・フォルダ名・パス・評価・作成/編集/取り込み日時・ファイルサイズ・プロンプト・ネガティブプロンプト・その他メタデータ・メモ欄を横断的に検索します。
  * **AND検索:** スペース区切りですべてを含む画像に絞り込み（例: `猫 夕焼け`）
  * **OR検索:** `OR`でどちらか一方を含む画像（例: `猫 OR 犬`）
  * **除外検索:** `-`を先頭に付けてその語を除外（例: `猫 -屋外`）
  * **フレーズ検索:** `"..."`でスペースを含む語をひとまとまりに（例: `"名称未設定フォルダ 2"`）
  * **評価で絞り込み:** `star1`〜`star5`、`★★★`、または`3`のような数値
  * **期間で絞り込み:** `c:`（作成）/`m:`（編集）/`i:`（取り込み）の接頭辞、`..`で範囲指定、片側のみや年・年月・年月日の粒度も可。例: `c:2026-07`（7月作成）、`c:2026-07-01..2026-07-31`（期間内）、`i:2026-07-30..`（30日以降取り込み）、`m:..2026-07`（7月以前編集）。他条件とAND可（例: `猫 c:2026-07`）。
  * 大文字/小文字・全角/半角の違いは自動で同一視されます。
  * **折りたたみ中**のフォルダの画像は検索対象外です（見出しに案内表示）。展開すれば検索できます。
  * 一致なしの案内は表示方法により異なります（フォルダ見出しは残ります）。通常表示・グループ表示では該当箇所に「検索結果は 0 件です」、全フォルダ折りたたみ時は「検索したいフォルダを開いてください」と一度だけポップアップ表示されます。
* **並べ替え:** 名前順・作成日順・編集日順・取り込み日時順・評価順・ファイルサイズ順から選択、昇順/降順は別ボタンで独立切替。設定は再起動後も記憶されます。リスト表示では並べ替え種類に応じた補足情報がファイル名の下に表示されます（名前順・評価順時は作成日時）。
* **リスト/グリッド表示:** グリッド表示ではサムネイルサイズを「小」「中」「大」から選択（「大」では場所・作成日時もサムネイル下に表示）。
* **フォルダ別グループ表示:** リスト表示中、画像をフォルダ別に見出しでグループ化（有効時はグリッド表示へ切替不可）。見出しにフォルダ名と画像枚数を表示し、クリックで折りたたみ/展開（▼/▶）。**右クリック**メニューには「並び順を編集」（D&D）、「一括で書き出す（連番リネーム）」（DB登録済み画像を採番ルールで連番リネームしコピー、未登録ファイルは対象外）、「フォルダ専用の自動採番を設定」（プレフィックス・桁数・アペンドをアプリ既定から上書き、取り込みとリネーム/書き出しに優先適用、チェックオフで復帰）、「データベースから削除」（実ファイルは無事、DBと同期対象から除外、再取り込みで復帰）があります。**Finder等へD&D**でフォルダごとコピー可能（常にコピーのみ）。表示状態は次回起動時まで記憶されます。

### 2.4 メタデータの確認・編集

* 右側の「メモ」「使用モデル / チェックポイント」「プロンプト」「ネガティブプロンプト」「その他パラメータ / メタデータ / EXIF情報」の各欄で、生成時の情報（カメラ写真はEXIF情報）やメモを確認・編集できます。
* 各欄見出し行の3ボタンで「表示/非表示」「コピー」「クリア」が可能。クリアやプロンプト編集は「変更を保存」を押すまで確定しません（誤操作しても安心です）。
* 画面右上の一括表示/非表示ボタンで、5つの編集欄と取り込み対象形式のチェックボックス欄をまとめて切り替えられます。
* 「ファイル名」は実際のファイル名、「名前」はアプリ内だけの表示名です。実ファイル名と別に自由に変更・重複でき、検索タグのようにも使えます。新規取り込み画像には既定で自動採番名（例: CG_00001）が付きます。

### 2.5 編集の保存・複製・削除

* **変更を保存:** 名前・評価・プロンプト等を保存。複数選択時は評価のみ一括保存も可能。
* **別名で保存:** オリジナルは変更せず、編集内容を新しいコピーとして指定場所に保存します。
* **編集ロック:** 右クリック→「編集をロック」。ロック中は削除・情報変更ができず、誤操作から守れます。
* **一括リネーム:** 複数選択して右クリック→「選択画像を一括リネーム（連番）...」。自動採番ルールでDB上の**「名前」欄のみ**を連番化（**実ファイル名は不変**、揃えたい場合は「一括で書き出す」）。フォルダ専用ルールがあれば優先、専用の一時カウンタで1から採番、`{フォルダ名}`があればフォルダごとに1から採番。編集ロック中の画像は対象外で、実行前に名前例の確認ポップアップが出ます。
* **一括で書き出す（連番でリネーム）:** 右クリック（複数選択可）→「選択画像を一括で書き出す（連番でリネーム）...」。書き出し先を選ぶと、元ファイルはそのままに**実ファイル名**を連番リネームしたコピーを作成（拡張子含む）。「コピー」でなく「書き出す」と呼ぶのは、Finderへの通常D&Dと区別するためです。
* 「リネーム時に編集画面を表示する」がオンの場合、一括リネームは「現在の名前」／「変更後の名前」、一括書き出しは「現在のファイル名」／「変更後のファイル名」と表記され対象が分かりやすくなります。同名・空欄の行は赤くハイライトされ、解消するまで実行できません。
* **削除:** 「選択した画像をリストから削除」でアプリのリストから除外します。実際のファイルは削除されず、削除した画像は以後「同期」を実行しても自動では復活しません（詳細は「[5. フォルダ画像の同期ルール](#5-フォルダ画像の同期ルール)」参照）。

### 2.6 スライドショー

「スライドショー」ボタンで自動再生を開始。速度は0.5倍〜2倍の範囲で0.25刻みに調整可能。検索で絞り込んだ状態で開始すると絞り込んだ画像だけを自動巡回します。

### 2.7 読書モード（見開き表示）

選択した画像と同じフォルダ内の画像を、本を開いたように2枚並べて全画面表示するモードです。プレビューエリアの「全画面」ボタン右隣の専用ボタンから起動します。

* **利用条件:** 「フォルダ別グループ表示」が有効な時、またはアルバム表示中にアルバム内の画像を選択している時のみ利用可能（それ以外はボタンがグレーアウト）。
* **並び順:** アプリの並べ替え設定に関わらず、常に「名前順」で表示。
* **開き方向:** 「左開き（左から右へ読む）」「右開き（右から左へ読む）」を切替。見開き表示中の変更はその場限りで、既定値は設定画面から変更します。
* **ページ操作:** 画面下部のスライドバー（見開き単位で移動）、最初/最後へのジャンプボタン、左右矢印キー、画面左右端（幅の約15%）のクリックに対応。進む/戻る方向は画面上の左右ではなく「読む方向」に従います（右開き時は→で戻り、←で進みます）。
* **背景の切り替え:** スライドバー左端のボタンでダーク/ライトを切替（アプリ本体のテーマとは独立、次回も記憶）。
* **中央詰め表示:** 背景切り替えボタン右隣で左右ページの並べ方を切替。通常は各ページを中央配置、オンにすると実際の本のように画面中央で突き合わせます（次回も記憶）。
* **現在の開き方向表示:** 画面右上に常時表示（▶左開き / ◀右開き）。
* 閲覧専用で編集はできません。「Esc」または終了ボタンで通常表示に戻ります。

---

## 3. アルバム（仮想フォルダ）機能

### 3.1 アルバムとは

実際のフォルダ構成とは別に、画像を自由にまとめられる仮想的な「アルバム」を作成できる機能です（v1.4〜）。1枚の画像を複数アルバムに同時所属させることもできます。通常の画像リスト表示とは併用せず、リスト上部の切り替えボタン（フォルダ／アルバムアイコン）でどちらか一方に切り替えます。

### 3.2 アルバムの作成

「アルバムを作成する」ボタンで、アルバム名とそのアルバム専用の自動採番ルールを1つにまとめたダイアログが開きます。

* アルバム名を入力（既定「Seed Book {次の番号}」、自由に変更可）。
* 「このアルバムの専用ルールとして設定する」で、そのアルバムだけの採番ルール（プレフィックス・桁数・アペンド）をその場で設定できます（既定オフ、オフ時はアプリ全体の既定ルールを使用）。
* プレフィックス・アペンドには`{アルバム名}`・`{日付}`（YYYYMMDD）が使えます（英語UIでは`{album name}`/`{date}`表記、意味・動作は同じ）。
* ルールは見出し行の右クリックメニュー「アルバム専用の自動採番」からいつでも変更できます。

### 3.3 画像のアルバムへの追加

* **右クリックメニューから:** 画像リスト（またはアルバム内）を右クリックし、「アルバムに追加」で既存アルバムを選ぶか、「新しいアルバムを作成...」でその場で作成しながら追加できます。1枚のみの追加でも、作成ダイアログで採番ルールを有効にしていれば即座に適用されます。
* **ドラッグ&ドロップで:** 画像を左側のアルバム見出し行へ直接D&Dして追加できます。
* いずれも複数選択時は同じ操作でまとめて追加できます。

### 3.4 アルバム見出し行の操作

画像リストの「フォルダ別グループ表示」と同じ操作感です。見出し行クリックで展開/折りたたみ（▼/▶）。右クリック（または右端の「⋯」アイコン）でメニューが開きます。

* **並び順を編集:** D&Dでアルバムの並び順を変更。
* **名前を変更:** アルバム名を変更。
* **すべて開く／折りたたむ:** 表示中の全アルバムを一括開閉。
* **一括で書き出す（連番でリネーム）:** アルバム内画像を採番ルールで連番リネームしつつ指定フォルダへコピー書き出し。
* **アルバム専用の自動採番:** そのアルバム専用の採番ルールを設定・変更・解除（[3.2](#32-アルバムの作成)参照）。
* **アルバムを削除:** アルバムの「入れ物」のみを削除（中の画像は削除されません）。

### 3.5 アルバム専用の自動採番ルールの適用範囲

設定すると、アルバム見出しメニューの「一括で書き出す」と、アルバム内画像を選択しての「一括リネーム」「連番でコピー書き出し」（1枚選択でも可）で優先して使われます。フォルダ専用ルールやアプリ全体の既定ルールとは完全に独立しており、同じ操作で両方が同時に参照されることはありません。アルバムは取り込み元になり得ないため、新規取り込み時の採番には影響しません。

### 3.6 Finderへの書き出しとファイル名の扱い

アルバムや展開中のアルバム内画像は、Finder（エクスプローラー）へ直接D&Dして書き出せます。アルバム自体は実フォルダを持たないため、ドラッグ開始時に一時フォルダへコピーして実体化し、Finder側には「フォルダ」として渡されます。

この方法では**元のファイル名がそのまま使われ、採番ルールは適用されません**。採番ルール適用済みコピーが欲しい場合は[3.4](#34-アルバム見出し行の操作)の「一括で書き出す（連番でリネーム）」をご利用ください。

### 3.7 検索・並び替えとの連携

アルバム表示中も、検索欄で開いているアルバム内の画像を絞り込み検索できます（該当なしなら「検索結果は0件です」、閉じているアルバムは「閉じているアルバムは検索対象外です」と見出し下に表示）。並び替えの種類・昇順/降順もそのまま反映されます。

---

## 4. 設定画面

画面右上の歯車アイコンから開きます。

<img width="296" height="537" alt="UI4" src="./design/screenshots/ui_settings_dark.png" />



* **自動採番・リネーム:** 「新規取り込み時に自動採番する」で機能のオン/オフを切替（オフでは実ファイル名（拡張子除く）がそのまま「名前」に）。オンではプレフィックス・桁数・アペンドを設定でき、組み合わせごとに採番は独立、削除しても番号は再利用されません。`{フォルダ名}`・`{日付}`（YYYYMMDD）が使え、変わると別連番として管理（英語UIでは`{folder name}`/`{date}`表記、動作は同一）。「プレースホルダー」リンクはカーソル位置の欄へ「_」区切りで挿入します。「保存」は確認ポップアップで「はい」を選ぶまで未確定。「採番のリセット」は番号を1に戻します（重複の恐れがあるため確認あり）。「リネーム時に編集画面を表示する」オンで、リネーム・書き出し前に名前一覧（元／変更後）を確認・編集でき、同名・空欄の行は赤くハイライトされ解消するまで実行不可。オフなら確認ポップアップに例を数件示すのみです。
* **表示項目:** Steps・Sampler・Scheduler・CFG scale・Seed・Sizeの個別表示を項目ごとにチェックボックスで切替。
* **外観モード:** 「自動（OS追従）」「ダーク固定」「ライト固定」を選択。同グループ内で左右パネル配置（「標準」＝検索・リストが左／「反転」＝右）も切替可能（反転時も設定ボタンは右上に固定）。
  * これら2つは設定画面を開かずウィンドウ右上のボタン（一括表示/非表示ボタンの右隣2つ）からもワンクリックで切替可能です。パネル反転ボタンは標準⇄反転をトグルし、外観モードボタンは「自動→ダーク→ライト→自動…」と巡回します（アイコンで状態が一目瞭然）。設定画面と設定を共有し、どちらから変更しても両方に反映されます。
* **表示言語:** 「自動（OS言語に追従）」「日本語」「English」から選択。次回起動時から反映されます（要再起動）。「自動」ではOSが日本語なら日本語UI、それ以外は英語UIです。
* **フォルダ取り込み時の順序:** 毎回確認するか、前回使った順序を自動で使うかを選べます。
* **読書モードの既定設定:** 開き方向（「左開き」「右開き」）と中央詰め表示の既定オン/オフを選べます（使い方は[2.7 読書モード（見開き表示）](#27-読書モード見開き表示)参照）。
* **データベース:**
  * **現在使用中のデータベースの表示・切り替え:** 一覧から選んで「データベースを変更」を押すと自動再起動して切り替わります。右のフォルダアイコンで保存フォルダをFinder（Windowsはエクスプローラー）で開けます。
  * **データベースを作成:** 用途ごとに複数のデータベースをデータ保存フォルダ内（`~/Library/Application Support/AIImageViewer/`）に作成できます（`image_metadata.db`、`image_metadata2.db`...）。作成しただけでは切り替わらず、一覧から改めて選択が必要です。
  * **画像一覧（CSV）:** 現在のデータベースの画像一覧をCSVに書き出せます。「基本情報のみ」「プロンプト等のメタ情報を含める」を選べます。
  * **同期履歴（CSV）:** 見つからなかったフォルダ・画像や取り込みエラーの履歴をCSVに書き出せます。詳細は[5.6 同期履歴](#56-同期履歴)参照。
  * **データベースを初期化:** 現在のデータベースから画像・フォルダ情報をすべて削除します（実ファイル・その他アプリ設定は保持）。
  * **データベースが見つからない場合:** 起動時に既定の保存先にファイルがない場合（初回起動・削除時）、選択ポップアップが表示されます。「新しい空のデータベースを作成する」（既定、初回OK）「同じフォルダ内の別のDBを選ぶ」「外部のバックアップファイルを読み込む」から選択。外部ファイルは有効性確認のうえ新しいDBとしてコピーされ通知されます。詳細は[6. データのバックアップ・移行](#6-データのバックアップ移行)参照。
    「新しい空のデータベースを作成する」選択時のみ「サンプルデータを読み込む」が表示され、チェックすると動作確認用のダミー画像が一緒に取り込まれます（通常の画像同様に削除可能）。
* **この設定をすべてリセット:** 命名規則・表示項目・テーマ設定などを初期値に戻します（自動採番のカウンタ自体はリセットされません）。

---

## 5. フォルダ画像の同期ルール

「フォルダを取り込む」で取り込んだフォルダはパスを記憶しておき、「🔄 同期」ボタンで再スキャンできます。この時の判定・挙動をまとめます。

なおここでの「名前」はフォルダやファイルのオリジナルの名前を指し、アプリ内の名前欄（自動採番のCG001、CG002等）とは異なります。

### 5.1 再同期の対象になる条件

取り込み時に記憶した絶対パスと完全に一致しているかどうかが唯一の条件です。Finderでの操作を例にすると、フォルダ名の変更・移動、画像ファイル自体の名前変更・移動は、いずれもパスが変わるため対象外になります。

💡 「名前を変えた」も「移動した」も、結局は「記憶していたパスと一致しなくなった」という同じ1つの状態です。

### 5.2 対象外になった場合、画像リストではどう見えるか

登録画像ファイルが実際にそのパスに存在するかを確認します（起動時・「同期」実行時に確認し、結果をリスト表示に反映）。

* 存在しなければ画像リストに表示されません。「同期」を押さなくても、フォルダ変更直後から自動反映されます。
* この時点ではDBレコードはまだ削除されず、一時的に「非表示」なだけです。
* 除外時は画面下部のステータスバーに「○○ 件の画像ファイルが見つからないため表示していません（外付けドライブ未接続の可能性があります）。」と一時的に案内されます（外付けドライブ未接続で消えたように見えるだけのケースに気づけるようにしています）。

### 5.3 データベース上の扱い（重要）

「🔄 同期」ボタンは新しい画像の取り込みだけでなく、存在しなくなった画像のレコードをデータベースから完全に削除する役割も持ちます。

| タイミング | 画像リストの表示 | データベースのレコード |
|---|---|---|
| パス変更直後（同期は未実行） | 表示されない | 残っている（一時的に非表示） |
| その後「同期」を実行した | 表示されない | 削除される（元に戻せない） |

「同期」を1度でも実行すると、その時点でパスが無効な画像は完全に削除されます。

### 5.4 元に戻す方法（復元）

**パターンA：まだ「同期」を実行していない場合** — フォルダ名・場所を元通りに戻せば、次に画像リストを開いた時点で自動的に再表示されます。再取り込みの操作は不要です。

**パターンB：すでに「同期」を実行してしまった場合** — レコードは削除済みのため、フォルダを元に戻しても自動では復活しません。「フォルダを取り込む」または「画像を取り込む」で改めて取り込み直す必要があり、その際以下の情報は失われ新規画像として登録し直されます:
* 評価（★）
* 編集ロック状態
* 「名前」欄に付けていた独自の名称（新しい自動採番名になります）
* 保存済みのプロンプト等の編集内容

### 5.5 まとめ（一言でいうと）
* フォルダを移動・改名しても、同期ボタンさえ押さなければ、元に戻すだけで画像は復活します。
* 一度「同期」を押すと、その時点で無効なパスの画像は完全に削除され、後戻りできません。
* 移動・改名を予定している間は、元に戻すまで「同期」ボタンを押さないのが安全です。

### 5.6 同期履歴
「同期」ボタンを押すたびに、見つからなかったフォルダ・画像や取り込みエラーが自動的に記録されます。

* **ポップアップは初回のみ:** 同じ問題が続く間、警告ポップアップは最初の1回だけで、2回目以降は履歴にのみ記録されます。
* **再発時は改めて警告:** 一度フォルダが見つかる状態に戻れば警告状態はリセットされ、再度見つからなくなれば改めて警告されます（意図的な移動やドライブの抜き差しを想定）。
* **履歴の見方:** 同期結果ポップアップの「履歴」ボタンで日時・種別・対象パス・内容を一覧でき、同じ内容は最新の1件のみ残ります。
* **CSVへの書き出し:** 履歴ダイアログ内、または設定画面の「同期履歴（CSV）」から。
* **保持件数:** 直近500件まで、超えた分は古いものから自動削除されます。

---

## 6. データのバックアップ・移行

### 6.1 データベースが見つからない場合の選択ダイアログについて
アプリ起動時に既定の保存先にデータベースファイルが見つからない場合（初回起動時、またはファイル直接削除時）、選択ポップアップが表示されます（詳細は「[4. 設定画面](#4-設定画面)」参照）。

**通常お使いの場合は「新しい空のデータベースを作成する」を選んでください。** これが既定の選択肢で、初めて使う場合はそのまま「OK」で問題ありません。他の2つは以前使っていたデータを復元したい場合のみ使う特殊なケース向けです。

### 6.2 データベースの保存場所
このアプリのデータ（画像の登録情報・プロンプト・評価・設定など）は以下のフォルダに保存されています。

```
~/Library/Application Support/AIImageViewer/
```

このフォルダ内に `image_metadata.db`（既定のデータベース）や、複数作成した場合は `image_metadata2.db` などが入っています。**画像ファイル自体はここには保存されません**（取り込み元のフォルダに置いたまま管理されます）。バックアップ・移行の対象はこのフォルダの中身です。

### 6.3 バックアップの取り方
1. Finderのメニューで「移動」→「フォルダへ移動...」を選び、`~/Library/Application Support/AIImageViewer/` と入力して開きます。
2. フォルダの中身（`.db` ファイル）を、外付けディスクやクラウドストレージなど任意の場所にコピーします。

定期的に、または大きな編集作業の前後にバックアップを取ることをおすすめします。

### 6.4 アプリの削除・再インストール時の注意
アプリ本体（`.app`）を削除しても、上記フォルダは自動では削除されません。再インストール後、同じMac・同じユーザーで起動すれば既存のデータベースがそのまま見つかります。完全にデータごと削除したい場合は、このフォルダも手動で削除してください。

### 6.5 別の環境（別のMac・別のユーザーアカウント）へ移行する場合
1. 移行元で6.3の手順に従い `.db` ファイルをコピーしておく
2. 移行先でアプリを一度起動する（データベースが見つからない選択ダイアログが表示されます）
3. 「外部のバックアップファイルを指定して読み込む」を選び、コピーした`.db`ファイルを指定する
4. 読み込み完了後、新しいデータベースとして複製され通知が表示されます

**注意:** 画像ファイル自体はデータベースに含まれません。取り込み元のフォルダごと同じパス構成で移行先にも用意しないと「見つからないファイル」が残るため、可能であれば画像フォルダとデータベースを一緒に移行してください。

---

## 7. 既知の制限事項

**ドラッグ&ドロップでの取り込みは一時的に無効化中**

外部（Finder等）から画像リストへ直接ドラッグ&ドロップして取り込む機能は、動作が不安定なため一時的に無効化しています。画像の取り込みは「フォルダを取り込む」「画像を取り込む」ボタンをご利用ください。

**フォルダ別グループ表示で、折りたたみ操作後に画像送り（前へ/次へ）が反応しないことがある**

表示中の画像を含むフォルダを折りたたんだ後、画像送り（前へ/次へボタン・キーボード操作）が正しく反応しないことがあります。致命的な不具合ではないため、原因調査・修正は今後の課題として保留しています。
