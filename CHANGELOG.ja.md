📖 このCHANGELOGと連動した開発の進捗・雑感は [開発日記](https://ocoe-puipui.github.io/diary/) でも公開しています。

## 目次（更新日順）

* [2026-08-16](#--2026-08-16)
* [2026-08-15](#--2026-08-15)
* [2026-08-14](#--2026-08-14)
* [2026-08-09](#--2026-08-09)
* [2026-08-05](#--2026-08-05)
* [2026-08-04](#--2026-08-04)
* [2026-08-03(2)](#--2026-08-032)
* [2026-08-03](#--2026-08-03)
* [2026-07-30](#--2026-07-30)
* [2026-07-29](#--2026-07-29)
* [2026-07-28](#--2026-07-28)
* [2026-07-26](#--2026-07-26)
* [2026-07-25](#--2026-07-25)
* [2026-07-23](#--2026-07-23)
* [2026-07-22](#--2026-07-22)
* [2026-07-21](#--2026-07-21)
* [2026-07-20](#--2026-07-20)
* [2026-07-19](#--2026-07-19)

---

## - 2026-08-16
---
### RELEASE
* **v1.2.0** をこの時点のコードでリリース（メモ欄・EXIF情報表示・フォルダ専用の自動採番・一括リネームと一括書き出しの分離・プレースホルダー改善などを含む）

### ADDED

## 上部バー左端にバージョン番号表示を追加（v1.3.0向け）
* アプリ名仮表示ラベルの撤去で空いた上部バー左端に、控えめなバージョン番号（例: `v1.3.0`）を表示
* バージョンを`version.py`の`APP_VERSION`に一元化し、`app.py`と`AIImageViewer.spec`の両方から参照

## アプリ名を「Seed Book」に正式決定
* 「AI Image Viewer & Manager（仮称: Seed Book）」から「Seed Book（AI image browser）」に正式決定
* Dock・ウィンドウタイトル・.appファイル名・DMGボリューム名・ヘルプ・README・マニュアルのタイトルをすべて新名称に統一
* `.spec`内の実行ファイル名（EXE/COLLECTの`name=`）はスペースなしの`SeedBook`（Finder上の`.app`表示名には影響なし）
* 名称検討中の仮表示ラベル（v1.1.0で追加）は撤去。Bundle Identifierは開発用リポジトリでは現行のまま、配布用は`com.ocoe-puipui.seedbook`を使用予定

## 表示言語切り替え（日本語／English）に対応
* 設定画面に「表示言語」項目を追加（自動／日本語／English、変更は次回起動から反映）
* 外部翻訳ライブラリに依存せず、Python辞書（`i18n.py`）で文言を自前管理（既存の設計方針と一貫性を重視）
* メイン画面・設定画面・各種サブダイアログ・アプリ内ヘルプの表示文言を英語対応（日英360件超のキーを整備）
* 自動採番のプレースホルダーに英語表記（`{folder name}`/`{date}`）を追加し、表示言語をまたいでも同じ意味に解決されるようにした
* マニュアルを言語別に分離（`manual.md`＝英語版、`manual.ja.md`＝日本語版）し、相互リンク・表示言語連動の参照切替に対応

## メモ欄の配置位置を変更
* メモ欄を「その他パラメータ」欄の下から、使用モデル欄の上へ移動

## 自動採番プレースホルダーの挿入先修正
* プレースホルダー（{日付}/{フォルダ名}）が常にアペンド欄へ挿入されてしまう不具合を修正し、フォーカス中の欄へ正しく挿入されるようにした
* プレフィックス欄への挿入時は区切りの「_」を後ろに（例: `{フォルダ名}_`）、アペンド欄は従来通り前に付ける

## メモ欄を固定表示からスクロール追従に変更
* メモ欄を他のメタデータ欄と同じスクロールエリア内へ移動し、ボタン位置のズレを解消
* 見出し行の生成方法を他欄と共通化し、最上段アイコンの並びも整理（専用の表示/非表示ボタンは一括ボタンへ統合）

## 画像ごとのメモ欄を追加
* 保存・スライドショー行の直上に、画像ごとの自由記入メモ欄（1行）を追加。プロンプト等には影響せず専用カラムに保存
* コピー・クリアボタン、表示/非表示切替に対応。検索対象にも追加し、「別名で保存」時も内容を引き継ぐ

## 取り込み形式・メタデータ編集欄に表示/非表示ボタンを追加
* 取り込み対象形式のチェックボックス欄、画面右上のメタデータ編集欄4つをまとめて表示/非表示に切り替えられるボタンを追加

## カメラ写真のEXIF情報表示（欄名を「その他パラメータ / メタデータ / EXIF情報」に変更）
* AI生成パラメータが見つからない画像（JPEG/TIFF/WEBP）について、メーカー・機種・撮影日時・露出・ISO・GPS等のEXIF情報を自動表示（PNG/GIF/BMPは非対応）
* CSVエクスポートの項目名も同様に変更

## - 2026-08-15
---
### ADDED

## 自動採番のプレフィックス／アペンドにプレースホルダー対応を追加
* `{フォルダ名}`・`{日付}`（YYYYMMDD）が使用可能に。置換後の文字列単位で採番カウンタが分かれる
* 設定画面のプレビュー表示も置換結果を表示。今後追加する命名系機能でも同じ置換ロジックを共通利用予定

## 選択画像の一括リネーム機能を追加
* 複数選択して右クリック→「選択画像を一括リネーム（連番）...」で、実ファイル名・「名前」欄をまとめて連番変更
* 番号はこの操作専用の一時カウンタで1から採番（永続カウンタには影響なし）。ロック中の画像は対象外

## 連番リネームコピー機能を追加
* 右クリックメニュー（画像・フォルダ見出しとも）に「一括で書き出す（連番でリネーム）...」を追加。元ファイルはそのまま、連番リネームしたコピーを作成
* 一括リネームと共通の採番ロジックにリファクタリングし、番号の振り方を一貫させた

## 設定画面「自動採番・リネーム」グループの表記整理・プレースホルダー挿入リンクを追加
* グループ名・チェックボックス表記を整理し、「リネーム時に編集画面を表示する」を新設
* プレフィックス・アペンド欄の横にプレースホルダー挿入リンクを追加し、レイアウトの不具合も修正

## 一括リネーム／一括書き出し実行前の確認・編集画面を実装
* 「元のファイル名／変更後の名前」の一覧をダブルクリックで直接編集できる確認ダイアログを新設。重複・空欄は赤くハイライトして実行をブロック

## 全画面プレビューの不具合修正・表示調整
* プレビューが「非表示」のまま全画面にすると何も表示されない不具合を修正。余白も切り詰めて画像を大きく表示

## フォルダ見出し行のドラッグ＆ドロップによるフォルダコピー機能を追加
* フォルダ別グループ表示中、見出し行をFinder等へドラッグ＆ドロップすると実フォルダごとコピーできるように（常にコピーのみ許可）

## フォルダ見出し行の右クリックメニューに「フォルダをデータベースから削除」を追加
* フォルダと中の画像をまとめてDBから削除（実ファイルは削除せず、再度取り込めば同期対象に戻る）

### FIXED
## 一括リネーム編集画面の不具合修正
* セル直接編集がシグナルを再発火させフリーズする不具合を修正。表セル編集を廃止し、行選択→下の「名前」欄で編集する方式に変更
* 選択時の配色不具合も併せて修正
* 「一括で書き出す」等、複数のファイル/フォルダ選択ダイアログが既定でソースコードのフォルダを開いていた問題を修正し、デスクトップを既定にした

## フォルダ専用の自動採番機能を追加
* フォルダ見出しの右クリックメニューに「フォルダ専用の自動採番を設定...」を追加し、そのフォルダだけアプリ全体既定を上書き可能に
* 新規テーブル`folder_naming_rules`で管理し、新規取り込みだけでなく一括リネーム・一括書き出しにも優先適用

## 「一括リネーム」の対象を「名前」欄のみに変更（実ファイル名は変更しない）
* 従来は実ファイル名も変更していたが、「名前」欄のみを対象とする方針に変更（実ファイル名の変更は「一括で書き出す」を利用）
* 確認・編集画面の表記も、対象に応じて「現在の名前」／「現在のファイル名」に分けて分かりやすくした

---

## - 2026-08-14
---
### ADDED

## 同期時の「見つからないフォルダ／画像」「取り込みエラー」を履歴として記録
* 同じ問題が続く間は初回のみポップアップ警告し、以降は履歴にのみ記録するよう変更（再度見つからなくなった場合は改めて警告）
* 同期結果ポップアップに「履歴」ボタンを追加し、日時・種別・対象パス・内容を一覧表示。CSVエクスポートにも対応
* 履歴は直近500件まで保持し、超過分は自動削除

## プレビューエリアでの左右キー送り
* プレビューエリアをクリックしてフォーカスすると、以後は左右キーで画像送りが可能に（他の入力欄からフォーカスを奪わないようクリック起点に限定）

## 上部バーに「パネル反転」「外観モード」のワンクリック切り替えボタンを追加
* ヘルプアイコンの右隣に2つのボタンを追加。設定画面を開かずに左右パネル配置・外観モードを切替可能
* 設定画面の既存項目と設定を共有し、どちらから変更しても即時反映

### FIXED
## 同期履歴機能の細かい不具合を修正
* ダークモードでの文字色不具合、同一エラーの重複記録、記録時刻のUTCずれ（日本時間より9時間過去に表示）を修正
* CSVエクスポートの保存先ダイアログが既定でデスクトップを開くよう修正

### CHANGED
## 細かい表示調整
* エクスポートボタンの表記を簡略化し1行に2個並べる配置に変更
* 自動採番の「桁数」欄から見づらい上下矢印ボタンを廃止し、範囲の注記を追加

---

## - 2026-08-09
---
### ADDED

## プレビューエリアの「非表示」表示モード追加
* プレビュー表示サイズ切替に「非表示」を追加。選ぶとプレビューと前後移動ボタンが隠れ、メタデータ編集エリアが広く使える

## プレビュー画像のドラッグ＆ドロップによる取り出し機能を追加
* プレビュー画像をFinder等の外部アプリへ直接ドラッグ＆ドロップで取り出し可能に（常にコピーのみ許可）

## 左右パネルの配置切替（標準／反転）機能を追加
* 検索・取り込み・リスト等の左右配置を選べるラジオボタンを追加。設定ボタンは切替の影響を受けない位置へ移設

### CHANGED

## 設定画面のデータベースグループのレイアウトを整理
* 「新しいデータベースを作成」ボタンを選択フォームと同じ行へ移動し、3要素を均等幅で表示
* ボタン表記を簡略化（「データベースを作成」「データベースを変更する」）

## ヘルプボタンをメインウィンドウへ移動、リリースノートボタンを非表示化
* 「ヘルプを表示」をメインウィンドウの設定ボタン左へ移動。リリースノートは利用頻度が低いため非表示化（コードは温存）

## ボタン・選択肢の表記を簡略化
* 「データベースを変更する」→「データベースを変更」等、複数の表記を簡略化

## プレビュー操作行・保存行を編集エリアのスクロールに追従しないよう修正
* プレビュー切替行・保存/スライドショー行をスクロールエリアの外側へ移動し、スクロールしても隠れないようにした

## Seedのコピーボタンを追加、生成パラメータ表示を使用モデル欄の表示/非表示に連動
* Seed値の横に専用コピーボタンを追加し、生成パラメータ表示行を使用モデル欄の表示/非表示に連動させた

## 仮の将来のアプリ名称「Seed Book」をメインウィンドウ上部に表示
* 正式名称変更までの仮表示として上部バー左端に表示（混乱を避けるためバージョン番号は非表示）

---

## - 2026-08-05
---
### CHANGED

## dmg配布タスク7: .dmgファイルの作成 完了
* `hdiutil create`で`AIImageViewer.dmg`を作成し、署名・公証・ステープルまで完了
* 実機でマウント・警告なし起動・`spctl`での公証確認済み。成果物: `dist/AIImageViewer.dmg`

## dmg配布タスク8: 別ユーザーアカウントでのクリーン環境テスト 完了
* 検証用アカウントでAirDrop経由の配布・公証機能・初回起動フロー・ユーザーごとのデータ分離・基本機能を確認

## dmg配布タスク9: サンプルデータの同梱設計と実装 完了
* A1111/NovelAI/ComfyUI形式のメタデータを埋め込んだダミー画像7枚を`sample_data/`に同梱
* 「データベースが見つかりません」ダイアログに「サンプルデータを読み込む」チェックボックスを追加し、DB作成後に取り込む仕組みを実装
* `get_resource_dir()`を新設し、直接実行時・`.app`実行時の両方でサンプル画像の場所を解決できるようにした

## 開発フォルダの整理・sync_deploy.command修正
* `sync_deploy.command`のコピー漏れ（`sample_data/`）を修正し、不要な生成物・一時ファイルを削除

## 追加機能38: 「変更を保存」ボタンの反応をわかりやすく
* 保存成功時にステータスバーへ完了メッセージを表示し、保存ボタンも一時的に「✓ 保存しました」表示に切替
* ステータスバーの表示位置・表示時間を調整
* dmg配布タスク10（サンプル入り完成版の再ビルド・再配布）完了

## - 2026-08-04
---
### CHANGED

## 追加機能 36: データベース・設定の保存先を Application Support 配下へ移行
* DB・ポインタファイルの保存先をプロジェクトフォルダから`~/Library/Application Support/AIImageViewer/`へ変更（dmg配布に向けた準備）
* 初回起動時は旧フォルダから自動でコピー移行。全DB接続を絶対パス接続に統一し、`.app`化後もDB接続が壊れない設計に

## 追加機能 37: データベース未検出時の選択ダイアログ
* 既定の保存先にDBファイルがない場合、自動作成をやめ「新規作成」「別DBを選ぶ」「外部バックアップを読み込む」の選択ポップアップを表示
* 外部ファイルは検証のうえ新しいスロット名でコピーして使用

## ドキュメント更新: マニュアルへのバックアップ・移行手順の追加
* manual.mdに「データのバックアップ・移行」章を新設し、保存場所・バックアップ方法・移行手順を記載

## ドキュメント更新: README目次の追加、アプリ内ヘルプの章構成をマニュアルに統一
* README.mdに目次を追加し、アプリ内ヘルプもマニュアルと同じ6章構成に再編成

## dmg配布タスク2〜5: requirements.txt作成／アイコン確定／ビルドツール選定／.app生成
* `requirements.txt`（PySide6==6.10.3、Pillow==11.3.0）を作成
* アプリアイコン`AppIcon.icns`を確定
* `PyInstaller`を採用し`AIImageViewer.spec`を新規作成、`dist/AIImageViewer.app`のビルド・起動確認に成功

## 細かい修正・整理
* DB保存先移行に伴う文言の修正漏れ（確認ダイアログ・README）を修正
* `.gitignore`にビルド生成物・状態ファイルを追加、旧DBファイルを削除

## 開発ワークフローの自動化: 同期・デプロイランチャー
* `sync_deploy.command`を新規作成。リポジトリ用フォルダへの同期、テスト運用フォルダへのコピーを選択実行できるようにした

## dmg配布タスク6: コード署名・公証 完了
* Developer ID証明書を取得し、`entitlements.plist`を新設。再署名・検証・公証申請・ステープルまで完了し、Gatekeeper警告なしで起動できることを確認

---

## - 2026-08-03(2)
---
### CHANGED

## 追加機能 35: ツールチップ・ヘルプ文言の見直し
* 対応形式表示・スライドショー・データベース関連のツールチップを実態に合わせて修正・簡潔化

---

## - 2026-08-03
---
### CHANGED

## 追加機能 33: 自動採番ルール保存時の確認ポップアップ
* 「自動採番ルールを保存」に確認ポップアップ（はい/いいえ）を追加

## 追加機能 34: データベース保存フォルダを開くボタンを追加
* 現在使用中DBの表示欄にフォルダアイコンのボタンを追加し、保存フォルダをFinderで開けるように。保存先取得を専用関数経由に統一し、将来の保存先変更に備えた

---

## - 2026-07-30
---
### CHANGED

## 追加機能 28: 重複画像の取り込み可否を選べるように
* 「中身が同じ画像も取り込む」チェックボックスを追加（既定オフ）。判定をDB制約からアプリ側へ変更し、既存DBも自動移行

## 追加機能 29: 取り込み順序に「ファイル名逆順」を追加

## 追加機能 30: 検索対象に実際のファイル名（拡張子含む）を追加

## 追加機能 31: フォルダ別グループ表示中はグリッド表示ボタンを無効化

## 追加機能 32: フォルダ別グループ表示の並び順を編集できるように
* フォルダ見出しの右クリックから専用ダイアログを開き、ドラッグ&ドロップで並び替え・保存可能に（設定画面には置かず右クリック導線に一本化）

## 追加機能 33: フォルダ別グループ表示のON/OFF・折りたたみ状態を記憶

## UI微調整
* 設定画面のコンボボックス・ボタンの高さを揃えた

---
### FIXED

## フォルダ別グループ表示の状態を復元すると起動時にクラッシュする不具合
* 原因: テーマ色決定前に画像一覧読み込みが実行され、色未設定のまま見出しを描画しようとしていた
* 対応: テーマ適用処理を画像一覧の初回読み込みより前に実行する順序へ修正

---
### CHANGED

## デザインの見直し: 絵文字からSVGアイコンへの統一
* 右クリックメニュー等の絵文字表示を実際のアイコンに置き換え、評価表示の表記ゆれも統一

## 危険操作ボタンの配色を縁取り型に変更
* 削除・リセット系の4ボタンを、塗りつぶし配色から縁取り型デザインへ変更し、印象を和らげた（十分なコントラスト比を確保）

---

## - 2026-07-29
---
### FIXED

## メインウィンドウを閉じても全画面プレビュー・読書モードが残る不具合
* 原因: 独立トップレベルウィンドウのため自動で閉じない構造だった
* 対応: `closeEvent`で開いていれば一緒に閉じるようにした

---
### CHANGED

## 危険操作ボタン4種の配色統一・ソフト化
* 不統一だった配色を統一し、強い赤から落ち着いたテラコッタ系へ変更。専用色に分離して影響範囲を限定

## ボタン名・ツールチップの全体最適化
* 全37ボタンを棚卸しし、未設定だったツールチップの大半に説明文を追加。ボタン名も3件変更

## 設定ダイアログのさらなる調整
* 表示項目チェックボックスをグリッド化、ダイアログ最小幅を縮小、既定の高さをメインウィンドウに統一

## 追加機能 27: 読書モードの中央詰め表示
* 見開き両ページを画面中央で突き合わせて表示するオプションを追加し、設定として記憶

---

## - 2026-07-28
---
### CHANGED

## 追加機能 25: 読書モード（見開き表示）
* 同フォルダの画像を本のように2枚並べて全画面表示する新機能を追加（「フォルダ別グループ表示」有効時のみ）
* 左開き/右開きの切替、スライドバー・矢印キー・画面端クリックでのページめくりに対応。閲覧専用で、既存の全画面プレビューとは独立した専用クラスとして実装

## 追加機能 26: 読書モードの背景テーマ（ダーク/ライト）切り替え
* 読書モード全体の配色を切り替えるボタンを追加し、アプリ本体とは独立した設定として記憶

## 設定ダイアログのUI最適化
* 項目増加で縦長になっていた設定ダイアログを複数項目の横並び化・スクロールエリア化で改善し、高さの上限を画面の85%に設定

---

## - 2026-07-26
---
### 試みたが撤回した実装（記録として残す）

## OSユーザーごとのデータベース自動分離 → 撤回
* 目的: 同じMacの複数OSユーザーでアプリ本体を共有しつつDBだけ自動分離したいという要望への対応
* 問題: `.app`起動時のみ`sqlite3.DatabaseError: authorization denied`が発生。`com.apple.provenance`拡張属性によりmacOSのTCCが未署名`.app`からのアクセスを拒否していたことが原因
* 対応: 関連する変更一式を撤回し、変更前の状態（単純な相対パス指定）に戻した
* 教訓: `~/Library`配下などプロジェクト外の保存先は`.app`起動時にセキュリティ機能の影響を受けるリスクがあり、次の「複数DB切り替え機能」ではプロジェクトフォルダ内限定の設計に変更した

---
### CHANGED

## 追加機能 24: 複数データベースの作成・切り替え（プロジェクトフォルダ内限定）
* 上記の教訓を踏まえ、プロジェクトフォルダ内だけで完結する設計で再実装
* `image_metadata.db`等の複数DBを作成でき、使用中DBは`current_db.txt`で記録。切り替え時はアプリが自動再起動

## 設定ダイアログの調整
* 表示項目チェックボックスを横並びに変更し、重複する「閉じる」ボタンを廃止

---
### 除外設定
* `.gitignore`に`current_db.txt`の追加、`image_metadata.db`指定の`image_metadata*.db`への更新を推奨

---

## - 2026-07-25
---
### CHANGED

## 追加機能 19: 連番命名規則のオン/オフ切り替え
* オンなら従来通り連番付与、オフなら実ファイル名をそのまま「名前」として取り込む

## 追加機能 20: 画像一覧のCSVエクスポート機能
* 「基本情報のみ」「基本情報＋メタ情報」から選択可能。UTF-8 with BOMでExcel文字化けを回避

## 追加機能 21: 「名前」フォームのツールチップ
* アプリ内だけの名前であり自由に変更・重複可能である旨を表示

## 追加機能 22: ヘルプダイアログの改善
* 専用ダイアログに変更し、サイズ拡大・スクロール対応、マニュアルへのリンクを追加

## 追加機能 23: UIボタンの配色見直し
* 「採番リセット」を危険操作の配色に統一、CSVエクスポートボタンに緑を追加

## ドキュメント整備
* `manual.md`を実際の仕様に基づいて大幅拡充。UI配色・カーソルオーバー文言の一覧資料を新規作成

---
### FIXED

## フォルダ別グループ表示を折りたたんだ状態で画像送りするとアプリがフリーズする不具合
* 原因: 折りたたみ中の画像が選択行から消え「未選択」になり、探索ループの終了条件と一致せず無限ループになっていた
* 対応: 折りたたみ中の画像も非表示にするだけの方式に変更し、探索ループ自体も件数上限で必ず終了する構造に修正

---
## - 2026-07-23
---
### 提案・未実装のアイデア（IDEAS / PROPOSED）
今回の会話で検討したもので、まだ実装していないアイデアを記録しておく。

## 画像一覧のCSVエクスポート機能
* 標準の`csv`モジュールで実装予定。エクスポート項目・対象範囲・設置場所は未確定

## 「名前」フォームのツールチップ追加
* 文言は確定済み（自由に変更・重複可能、初期値は自動採番）

## 対応画像形式の分かりやすい案内
* 「フォルダを取り込む」ボタン・ヘルプ画面に対応形式（PNG/JPG/JPEG/WEBP）を明記予定

---
## - 2026-07-22
---
### CHANGED

## 追加機能 17: フォルダ別グループ表示の折りたたみ機能
* 見出し行クリックで画像一覧を折りたたみ/展開（▶/▼で状態表示）、フォルダごとに独立して保持
* 実装方式: 埋め込みボタン方式は不具合が出たため撤回し、見出し行全体をクリック対象とする方式を採用

## 追加機能 18: ComfyUI形式のメタデータ対応
* `workflow`/`prompt`のJSONを検出し「その他パラメータ」欄に保存。構造化抽出はせず生JSONを保持する方針
* 従来のAutomatic1111 / Forge / NovelAI形式への影響がないことを確認済み

---
### FIXED

## 新規データベース作成時の設定初期値シード処理の欠落
* 原因: `init_db()`内の既定値書き込み処理が失われていた。対応: 移行処理を復元

## 生成パラメータ個別表示のデフォルト値の巻き戻り
* 既定値を「非表示（0）」に修正

---
## - 2026-07-21
---
### CHANGED

## 追加機能 14: メタデータ編集欄の表示/非表示切替
* 各見出し行に表示/非表示切替ボタンを追加し、非表示分の高さを残りの欄へ自動配分

## 追加機能 15: 確認ポップアップのアイコン統一
* 標準アイコンから専用SVGアイコンに統一し、共通ヘルパー関数`show_confirm_dialog()`を新設

## 追加機能 16: 区切り線の追加
* プレビューエリアと操作ボタン行の間、各メタデータ見出しの上に薄い区切り線を追加

## UIデザインシートの作成
* UI要素の表示名・変数名・objectNameを一覧化した`design_sheet.md`を新規作成

---
### FIXED

## フォルダ別グループ表示の見出しがライトモードで見えなくなる不具合
* 原因: QSSの文字色指定の優先順位により、ライトモードでのみコントラスト不足に。対応: 見出し背景色をテーマ専用色に変更

## 空リスト/ドラッグ中プレースホルダーの背景色
* 周囲と同化していた背景色を、入力欄と同じ配色に修正

## ドラッグ&ドロップでの画像取り込みを一時的に無効化
* 動作が不安定だったため無効化し、取り込みボタンに一本化（コードは温存）

## 標準サイズのプレビュー画像が画像送りのたびに肥大化する不具合
* 原因: sizeHintがレイアウト計算に含まれフィードバックループが発生。対応: `QSizePolicy.Ignored`に変更

## メタデータ編集欄コピー機能の実装中に混入したコード構造の破損
* 誤って別メソッドに迷い込んでいたコードを正しい位置に修正

---
### 余白の微調整
* 「名前」欄まわり・編集エリア・プレビューエリアの間隔を複数箇所で調整

---
### 提案・未実装のアイデア（IDEAS / PROPOSED）更新
* 「アイコンのテーマ別配色ルール」は確認ポップアップにも対応し部分的に実装済みに
* 残りの項目（採番状況一覧、名前プレビュー、設定のエクスポート/インポート）は引き続き未着手

---
## - 2026-07-20
---
### CHANGED

## 追加機能 9: アイコンデザインの本格導入（Material Symbols）
* Google Material Symbolsを組み込み、絵文字アイコンを40種類以上置き換え
* 見出し・ラベル・ドロップダウン項目にもアイコンを追加し、アクセントカラー背景上では白色に自動切替

## 追加機能 10: 生成パラメータの個別表示
* Steps・Sampler・Scheduler・CFG scale・Seed・Sizeを使用モデル欄の下に読み取り専用表示し、項目ごとに表示/非表示を切替可能に

## 追加機能 11: 命名規則のアペンド欄、組み合わせ別の採番、採番リセット
* 「アペンド」欄を追加し、プレフィックス＋アペンドの組み合わせごとに採番カウンタを独立させ、「採番リセット」ボタンを追加

## 追加機能 12: 設定のリセット・データベースの初期化
* 「この設定をすべてリセット」「データベースを初期化」ボタンを追加し、実行前に確認表示

## 追加機能 13: デザイントークンの導入
* 余白・フォントサイズ・アイコンサイズを共通定数として整理し統一

---
### FIXED

## 設定メニュー内、連番桁数スピンボックスの視認性（ダークモード）
* 背景色と文字色が重なっていた不具合を、他の入力欄と同じテーマ対応スタイルで修正

## スライドショーボタンのアイコン
* 視認性の良くなかったアイコンを削除し、テキスト（▶/■）表示に戻した

---
### 提案・未実装のアイデア（IDEAS / PROPOSED）
今後の参考として、これまでの会話で提案したもののまだ実装していないアイデアを記録。

## UI統一性のさらなる改善
* アイコン運用ルールの明文化、テーマ別配色ルールの拡充、最小幅・余白ルールの体系化

## 機能面の改善案
* 採番状況の一覧画面、取り込み前の名前プレビュー、設定のエクスポート/インポート機能

---
## - 2026-07-19
---
### CHANGED

## 追加機能 1: 削除した画像を同期対象から除外
* `excluded_paths`テーブルで削除パスを記録し、同期時にスキップ。明示的な再取り込みで解除

## 追加機能 2: 画像の編集ロック機能
* 右クリックから画像単位で編集をロック/解除でき、ロック中は削除・編集欄をグレーアウト。ロックアイコンで判別可能に

## 追加機能 3: メタデータ欄にクリアボタンを追加
* 各コピーアイコン左に🗑ボタンを配置し、「変更を保存」まで確定しない一時的な変更として扱う

## 追加機能 4: サムネイルサイズ調整（小/中/大）
* グリッド表示のタイル幅・アイコンサイズを3段階から選択可能に（「大」は場所・日時も表示）

## 追加機能 5: フォルダごとのグループ表示
* リスト表示でフォルダ別に見出し行を挿入してグループ表示（グリッド表示中は無効）

## 追加機能 6: 画像リストへのドラッグ&ドロップ取り込み
* Finder等からのドラッグ&ドロップで画像・フォルダを取り込み可能に

## 追加機能 7: 「DB内表示名」→「名前」に変更、新規取り込み時のデフォルト値を連番に
* フィールド名を「名前」に変更し、初期値を実ファイル名から連番（例: CG_001）へ。削除しても番号は再利用しない

## 追加機能 8: 歯車アイコン+設定メニュー
* 連番命名規則編集・テーマ切替・ヘルプ・リリースノートリンクをまとめた設定メニューを追加

---
### FIXED

## 「変更を保存」ボタンの表示が戻らない不具合
* 原因: 複数選択時の保存ボタン状態をリセットする処理が削除時に抜けていた。対応: 削除後にもリセット処理を追加

## f-string構文エラーによる起動不能（Python 3.9環境）
* 原因: f-string内でのバックスラッシュ記法がPython 3.11以前では構文エラーになる。対応: バックスラッシュを`{}`の外に出す書き方へ修正

## フォルダグループ見出しのアイコン表示
* 絵文字埋め込みだった見出しを、OS標準のフォルダアイコン表示に変更

## 設定アイコンの位置・サイズ
* 分かりにくかった位置から画面右上（「名前」欄の右）へ移動し、サイズも拡大

---
### PLANNED（今後対応予定）

（現時点で未着手の項目はありません）

---
