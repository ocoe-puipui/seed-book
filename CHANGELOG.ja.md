# Changelog

## [1.4.1] - 2026-08-21
### Added

## プレビュー未選択時のプレースホルダーアイコンを追加、Folders/Albums行に一括展開/折りたたみトグルを追加
## 右クリックメニューに「リストから削除...」を追加し、左下の削除ボタンを選択状況に応じた文脈依存表示に変更
## 検索一致箇所のハイライト表示、検索対象外になった画像のプレビュー自動非表示/復元に対応
## 連番リネーム書き出し時に、サブフォルダを自動生成するか選べるオプションを追加
## プレビュー固定表示（ピン留め）機能を追加

### Fixed

## アルバム色「未設定（グレー）」の丸アイコンが表示されない不具合を修正
## 右クリックメニューでの削除誤操作、アルバム未選択時の削除ポップアップ誤表示を修正
## 表示言語切替がEnglish→日本語で1回のクリックで切り替わらない不具合を修正
## 上部バーアイコンの上下余白の不揃いを修正
## 画像取り込み結果ポップアップの英語未対応部分を修正
## Filename/Name入力欄・区切り線まわりの表示を整理（常に等幅化、区切り線の左右位置を統一、全画面プレビューのボタン行を中央揃えに変更）

### Changed

## 右クリックメニューのアイコンを用途ごとに区別、アルバムプロパティ表記を整理、並べ替え・編集欄まわりのUIを整理

## [1.4.0] - 2026-08-19
### Added

## アルバム（仮想フォルダ）機能を追加（v1.4向け・動作確認は次回に持ち越し）
* 実フォルダとは別に画像を横断的にまとめる「アルバム」機能を新設（1枚の画像を複数アルバムに所属可、画像リスト表示とは排他的に切替）。追加は右クリック「アルバムに追加」またはドラッグ&ドロップ。一覧は「フォルダ別グループ表示」と同じ操作感で、並び替え・名前変更・一括書き出し（連番リネーム）・専用自動採番設定・削除に対応。専用採番ルールはそのアルバムの書き出しにのみ独立適用。Finder等へのドラッグ&ドロップ書き出し（一時フォルダへ実体化）や、開いているアルバム内での検索絞り込みにも対応

### Fixed

## アルバム内の画像をキーボードの上下キーで移動してもプレビューが連動しない不具合を修正
* キーボード操作（`currentItemChanged`）が未対応だったのを共通ハンドラに整理して修正

## アルバム表示中に並び替えを変更しても即座に一覧へ反映されない不具合を修正
* 種類変更・昇順/降順切替の両方でアルバム一覧を即座に再構築するよう修正

## アルバム内の画像を複数選択した際、星評価の変更が保存されない不具合を修正
* 選択状態の参照先を`browse_mode`に応じて切り替えるよう修正

## アルバム表示中、プレビュー画面の画像送り（前へ/次へ）が機能しなくなっていた不具合を修正
* シグナルの発火元（`sender()`）を見て参照先の一覧を決めるよう修正

## アプリ内ヘルプの内容が、アルバム機能の追加以降更新されておらず現状と合っていなかった不具合を修正
* ヘルプに「3. アルバム（仮想フォルダ）機能」の章を新設し章番号を繰り下げ。折りたたみ後の画像送り不具合も既知の制限事項に追記（日英とも）

## 連番編集テーブルに、命名ルールのカスタマイズ方法を示す説明文がなかった点を修正
* 一括書き出し・一括リネームの確認/編集テーブルに、命名ルール変更は設定画面またはフォルダ/アルバムの右クリックから行う旨の案内文を追加

## 未保存の編集に気づかず画像送りで移動してしまう問題を修正
* 各種欄を変更すると「変更を保存」ボタンの色が変わり未保存とわかるようにした（保存または他画像選択で元に戻る）

### Changed（ユーザビリティ改善）

## 検索欄に、検索構文（OR検索・除外・期間指定など）を確認できるヒントアイコンを追加
* 検索欄右側に「？」アイコンを新設し、押すとアプリ内ヘルプを直接開けるようにした

## フォルダ/アルバムの表示切り替えボタンを、並び替え欄（ソートボタン）の上へ移動
* リスト下部の見つけにくい位置から移動。「アルバムを作成」ボタンも合わせて移動

## アルバム表示切り替えボタンのアイコンを、公園を連想させる形から「積み重なったレイヤー」の形に変更
* 分かりにくいとの指摘を受け"album_stack"アイコンに差し替え

## フォルダ/アルバムの見出し行に、右クリックメニューを開ける「⋯」アイコンを常時表示
* 右クリックメニューにしかなく発見しにくかったため、見出し右端に常時薄く表示

## 表示形式（リスト/グリッド）・サムネイルサイズ・プレビューサイズを、次回起動時にも復元するように
* この3項目だけDB未保存だったのを、`view_mode`/`grid_tile_size`/`preview_size_mode`をDBへ保存・復元するよう修正

## 表示モード（フォルダ/アルバム）・編集エリアの表示状態と並び順も、次回起動時に復元するように
* `browse_mode`/`metadata_fields_display`/`metadata_section_order`の設定キーを新設し`DEFAULT_APP_SETTINGS`にも登録

## ソートに「ファイル名順」を追加（「名前順」とは別の選択肢として区別）
* 従来の「名前順」はDB上の`file_name`列基準で実ファイル名とは別物だった。実ファイル名を基準に並べ替える方式を新設し選択肢に追加

## ドラッグ&ドロップ書き出しは元のファイル名のまま・採番ルールは適用されない旨を、見出しのツールチップとヘルプに明記
* ドラッグ書き出しと右クリック「一括で書き出す」の違いを見出しツールチップとヘルプに追記（日英とも）

## アルバム内の画像を選択しての一括リネーム・連番書き出しに、アルバム専用の自動採番ルールを適用
* 従来はフォルダ/全体既定ルールしか参照されなかったのを、アルバム専用ルール設定時は優先適用するよう修正。表示順の参照不具合も併せて修正

## アルバム表示中、展開中のアルバム内の画像を右クリックしてもメニューが出ない不具合を修正
* 画像行の右クリックメニューを`_show_image_row_context_menu`として共通化し、アルバム内画像行からも開けるようにした

## デザイン監査に基づく細部の調整（余白・アイコンサイズの統一）
* ステータス表示の上余白・見出し「⋯」アイコンサイズ・各種余白をデザイントークン定数へ統一（見た目上の変化はなし）

## サイドバーの正方形アイコンボタンのサイズを3規格（14/16/20px）に統一
* 12〜22pxでばらついていたサイズを`ICON_SIZE_MD/LG/XL`に統一。設定ボタンの外枠幅も他ボタンと揃えた

## フォルダ/アルバム切替ボタンがアクティブな時、アイコンの色がボタン背景（アクセントカラー）に馴染むように修正
* 他ボタンと挙動が揃っていなかったのを`SVG_ICON_COLOR_ON_ACCENT`で統一

## 検索欄横の構文ヒント「？」アイコンを撤去し、左側エリアの各行の縦幅をさらに統一
* ヒントアイコンを撤去（構文案内はヘルプから参照可）。各行の縦幅を36px/28pxに統一

## 「アルバムを作成する」ダイアログに、アルバム専用の自動採番設定を統合
* 名前入力と同じポップアップ内に採番項目（プレフィックス・桁数・アペンド・プレビュー）を追加（新設`AlbumCreateDialog`、既定オフ）

## 「アルバムを作成する」ダイアログの採番設定チェックボックスまわりを分かりやすく整理
* フォルダ用から流用していた分かりづらい説明文を撤去し、文言・配置を整理

## プレースホルダーに{アルバム名}を追加、アルバム起点の採番では{フォルダ名}の代わりにこちらを使用
* `resolve_naming_placeholders`に`album_name`引数を追加し、関連機能全体で一貫して渡すようにした

## 選択項目が1件のみでも一括採番（連番リネーム）を使えるように
* 従来は2件以上選択時のみ表示されていたメニュー項目を、1件選択時にも表示

## 「アルバムを作成する」で採番ルールを有効にした場合、作成と同時に追加した画像（1枚でも）へその場で連番を適用
* `_apply_naming_rule_to_image_ids`を新設しその場で適用するようにした

## アルバム関連の採番ダイアログで、既定値・ツールチップ・プレビュー文言が「フォルダ」表記のまま残っていたのを修正
* 初期値・ツールチップ・プレビュー文言をアルバム専用表記に差し替え

## 編集エリアの各セクション（メモ/使用モデル/プロンプト/ネガティブプロンプト/その他パラメータ）を並び替えられるように
* 各見出しに上下矢印ボタンを追加し入れ替え可能に。生成パラメータ表示は使用モデル欄と連動して移動、並び順はDB保存（`metadata_section_order`）

### Changed

## アルバム見出しアイコンを実フォルダと完全に同一のOSネイティブアイコンに変更
* 「完全に同じ見た目でよい」との要望により`SP_DirIcon`に統一

## 実フォルダ見出しアイコンを青、アルバム見出しアイコンを黄系のフォルダアイコンに変更
* 視覚的区別のため実フォルダを青、アルバムを黄系のSVGアイコンに変更し、描画サイズもリストと統一

## 「アルバムを作成」ボタンをソートボタンの下から、フォルダ/アルバム切り替えアイコンの直上へ移動

## 実フォルダ・アルバムの見出し行に、選択中を示す背景ハイライトを追加し、選択が続く間は維持されるように変更
* `active_header_folder_name`/`active_header_album_id`を新設し、別の選択までハイライトを維持

## アルバムの展開/折りたたみ状態を次回起動時にも記憶するように
* `database.get_collapsed_albums`/`set_collapsed_albums`を新設しフォルダ側と同様に永続化

## アルバム表示中のリスト下部ステータス表示を、アルバムの総数・画像の総数のみに変更
* 意味をなさなかった選択依存の表示を、ライブラリ全体の集計表示に変更

## 読書モード（見開き表示）を、アルバム内の画像を選択中も利用できるように
* 従来のフォルダ別グループ表示限定から、アルバム内画像選択時にも利用可能に拡張

### Known Issues（保留・今後の課題）

## フォルダ別グループ表示で、表示中の画像を含むフォルダを折りたたんだ後、画像送り（前へ/次へ）が反応しないことがある
* `select_item_by_id`等での修正を試みたが実機確認の結果症状は解消していない

## 連番編集テーブル内での命名ルールの直接編集（v1.4より後のバージョンで検討）
* 現状は個別編集のみで、命名ルール自体の変更は設定画面等から別途行う必要がある。今後の検討課題として記録のみ（README.ja.md/manual.ja.mdにも記載）

## [1.3.2] - 2026-08-18
### Release
* **v1.3.2** をこの時点のコードでリリース（ヘルプ内マニュアルリンクの修正）

### Fixed
## ヘルプ内のマニュアルリンクが404になる不具合
* `app.py`のURL定数がアプリ名正式決定（v1.3.0）時に更新されず非公開リポジトリを指したままだったため、3箇所を公開リポジトリ（seed-book）へ修正

## [1.3.1] - 2026-08-17
### Release
* **v1.3.1** をこの時点のコードでリリース（DB未検出時の起動クラッシュ修正）

### Fixed
## データベースファイルが存在しない状態で起動するとクラッシュする不具合
* 表示言語解決の`get_setting()`がDB未検出ダイアログより先に走り`sqlite3.connect()`が空ファイルを自動生成してしまうのが原因。言語解決をDB未検出処理・`init_db()`後に移動し、暫定言語は`i18n.detect_os_language()`で先に設定するよう修正

## 開発フォルダに残っていた移行元の旧DBファイルが、テスト時の自動移行ロジックを誤発動させる問題
* 開発フォルダ直下の旧`image_metadata.db`（実環境には影響なし）を削除して対応

## [1.2.0] - 2026-08-16
### Release
* **v1.2.0** をこの時点のコードでリリース（メモ欄・EXIF情報表示・フォルダ専用の自動採番・一括リネームと一括書き出しの分離・プレースホルダー改善などを含む）

## [1.3.0] - 2026-08-16
### Release
* **v1.3.0** を同日中に追加リリース（アプリ名「Seed Book」正式決定・表示言語切り替え対応などを含む）

### Added

## 上部バー左端にバージョン番号表示を追加
* アプリ名仮表示ラベル撤去で空いた位置に表示。バージョンは`version.py`の`APP_VERSION`に一元化

## アプリ名を「Seed Book」に正式決定
* Dock・タイトル・.app名・DMG名・ヘルプ・README・マニュアルを新名称に統一。実行ファイル名`SeedBook`、Bundle Identifier`com.ocoe-puipui.seedbook`を使用予定

## 表示言語切り替え（日本語／English）に対応
* 設定画面に「表示言語」項目を追加（自動／日本語／English、次回起動から反映）。Python辞書（`i18n.py`）で日英360件超のキーを管理し、採番プレースホルダーの英語表記追加・マニュアル言語分離も実施

## メモ欄の配置位置を変更
* 「その他パラメータ」欄の下から使用モデル欄の上へ移動

## 自動採番プレースホルダーの挿入先修正
* 常にアペンド欄へ挿入されていた不具合を修正し、フォーカス中の欄へ正しく挿入されるようにした

## メモ欄を固定表示からスクロール追従に変更
* 他のメタデータ欄と同じスクロールエリアへ移動しボタン位置のズレを解消。表示/非表示は一括ボタンへ統合

## 画像ごとのメモ欄を追加
* 自由記入メモ欄（1行）を追加、専用カラムに保存しコピー・クリア・表示/非表示・検索に対応

## 取り込み形式・メタデータ編集欄に表示/非表示ボタンを追加
* 取り込み形式チェックボックス欄とメタデータ編集欄4つをまとめて切り替えられるボタンを追加

## カメラ写真のEXIF情報表示（欄名を「その他パラメータ / メタデータ / EXIF情報」に変更）
* AI生成パラメータのない画像（JPEG/TIFF/WEBP）にEXIF情報を自動表示（PNG/GIF/BMPは非対応）。CSVエクスポート項目名も同様に変更

## [2026-08-15]
### Added

## 自動採番のプレフィックス／アペンドにプレースホルダー対応を追加
* `{フォルダ名}`・`{日付}`（YYYYMMDD）が使用可能に。置換後の文字列単位でカウンタが分かれ、設定画面のプレビューにも反映

## 選択画像の一括リネーム機能を追加
* 複数選択して右クリック→「選択画像を一括リネーム（連番）...」で実ファイル名・「名前」欄をまとめて連番変更（専用カウンタ、ロック中は対象外）

## 連番リネームコピー機能を追加
* 右クリックメニューに「一括で書き出す（連番でリネーム）...」を追加。元ファイルはそのまま連番リネームコピーを作成し、一括リネームと採番ロジックを共通化

## 設定画面「自動採番・リネーム」グループの表記整理・プレースホルダー挿入リンクを追加
* グループ名・チェックボックス表記を整理し「リネーム時に編集画面を表示する」を新設、挿入リンクも追加

## 一括リネーム／一括書き出し実行前の確認・編集画面を実装
* 「元のファイル名／変更後の名前」をダブルクリック編集できる確認ダイアログを新設。重複・空欄は赤くハイライトし実行をブロック

## 全画面プレビューの不具合修正・表示調整
* プレビュー「非表示」のまま全画面にすると何も表示されない不具合を修正し、余白も切り詰めて表示を拡大

## フォルダ見出し行のドラッグ＆ドロップによるフォルダコピー機能を追加
* フォルダ別グループ表示中、見出し行をFinder等へドラッグ&ドロップすると実フォルダごとコピー可能に（常にコピーのみ）

## フォルダ見出し行の右クリックメニューに「フォルダをデータベースから削除」を追加
* フォルダと中の画像をまとめてDBから削除（実ファイルは削除せず、再取り込みで同期対象に戻る）

### Fixed
## 一括リネーム編集画面の不具合修正
* セル直接編集がシグナルを再発火させフリーズする不具合を、行選択→下の「名前」欄で編集する方式に変更して修正。選択配色や既定表示フォルダの不具合も併せて修正

## フォルダ専用の自動採番機能を追加
* フォルダ見出しの右クリックに「フォルダ専用の自動採番を設定...」を追加し、そのフォルダだけ全体既定を上書き可能に（新規取り込み・一括リネーム・一括書き出しに優先適用）

## 「一括リネーム」の対象を「名前」欄のみに変更（実ファイル名は変更しない）
* 実ファイル名も変更していた従来仕様を「名前」欄のみに修正（実ファイル名変更は「一括で書き出す」を利用）

## [2026-08-14]
### Added

## 同期時の「見つからないフォルダ／画像」「取り込みエラー」を履歴として記録
* 同じ問題は初回のみポップアップ警告し以降は履歴のみ記録。「履歴」ボタンで日時・種別・対象パス・内容を一覧表示（CSVエクスポート対応、直近500件保持）

## プレビューエリアでの左右キー送り
* プレビューエリアをクリックしてフォーカスすると、以後は左右キーで画像送りが可能に

## 上部バーに「パネル反転」「外観モード」のワンクリック切り替えボタンを追加
* ヘルプアイコン右隣に2ボタンを追加し、設定画面を開かずに切替可能に

### Fixed
## 同期履歴機能の細かい不具合を修正
* ダークモードの文字色不具合、重複記録、記録時刻のUTCずれ、CSVエクスポート既定フォルダを修正

### Changed
## 細かい表示調整
* エクスポートボタン表記の簡略化と1行2個配置、自動採番「桁数」欄の上下矢印ボタン廃止と範囲注記の追加

## [1.1.0] - 2026-08-09
### Added

## プレビューエリアの「非表示」表示モード追加
* プレビューと前後移動ボタンを隠しメタデータ編集エリアを広く使えるモードを追加

## プレビュー画像のドラッグ＆ドロップによる取り出し機能を追加
* プレビュー画像をFinder等へ直接ドラッグ＆ドロップで取り出し可能に（常にコピーのみ）

## 左右パネルの配置切替（標準／反転）機能を追加
* 検索・取り込み・リスト等の左右配置を選べるラジオボタンを追加、設定ボタンは影響を受けない位置へ移設

### Changed

## 設定画面のデータベースグループのレイアウトを整理
* 「新しいデータベースを作成」ボタンを選択フォームと同じ行へ移動し3要素を均等幅表示。表記も簡略化

## ヘルプボタンをメインウィンドウへ移動、リリースノートボタンを非表示化
* 「ヘルプを表示」を設定ボタン左へ移動。リリースノートは利用頻度が低く非表示化（コードは温存）

## ボタン・選択肢の表記を簡略化
* 複数のボタン・選択肢の表記を簡潔なものに変更

## プレビュー操作行・保存行を編集エリアのスクロールに追従しないよう修正
* 該当の行をスクロールエリアの外側へ移動し、スクロールしても隠れないようにした

## Seedのコピーボタンを追加、生成パラメータ表示を使用モデル欄の表示/非表示に連動
* Seed値横に専用コピーボタンを追加し、生成パラメータ表示行を使用モデル欄の表示状態に連動させた

## 仮の将来のアプリ名称「Seed Book」をメインウィンドウ上部に表示
* 正式名称変更までの仮表示として上部バー左端に表示（混乱回避のためバージョン番号は非表示）

## [1.0.0] - 2026-08-05
### Changed

## dmg配布タスク7: .dmgファイルの作成 完了
* `hdiutil create`で`AIImageViewer.dmg`を作成し署名・公証・ステープルまで完了、実機確認済み

## dmg配布タスク8: 別ユーザーアカウントでのクリーン環境テスト 完了
* 検証用アカウントでAirDrop配布・公証・初回起動フロー・データ分離・基本機能を確認

## dmg配布タスク9: サンプルデータの同梱設計と実装 完了
* A1111/NovelAI/ComfyUI形式のダミー画像7枚を`sample_data/`に同梱し、「DB未検出」ダイアログから読み込めるように対応

## 開発フォルダの整理・sync_deploy.command修正
* `sync_deploy.command`のコピー漏れを修正し、不要な生成物・一時ファイルを削除

## 追加機能38: 「変更を保存」ボタンの反応をわかりやすく
* 保存成功時にステータスバーへ完了メッセージを表示し保存ボタンも一時的に切替。dmg配布タスク10（再ビルド・再配布）完了

## [2026-08-04]
### Changed

## 追加機能 36: データベース・設定の保存先を Application Support 配下へ移行
* DB・ポインタファイルの保存先をプロジェクトフォルダから`~/Library/Application Support/AIImageViewer/`へ変更（dmg配布準備）。旧フォルダからの自動移行と絶対パス接続への統一も実施

## 追加機能 37: データベース未検出時の選択ダイアログ
* 既定保存先にDBがない場合、自動作成をやめ「新規作成」「別DBを選ぶ」「外部バックアップを読み込む」の選択ポップアップを表示

## ドキュメント更新: マニュアルへのバックアップ・移行手順の追加
* manual.mdに「データのバックアップ・移行」章を新設

## ドキュメント更新: README目次の追加、アプリ内ヘルプの章構成をマニュアルに統一
* README.mdに目次を追加し、アプリ内ヘルプもマニュアルと同じ6章構成に再編成

## dmg配布タスク2〜5: requirements.txt作成／アイコン確定／ビルドツール選定／.app生成
* `requirements.txt`作成、アプリアイコン確定、`PyInstaller`採用で`.spec`新規作成、`.app`ビルド・起動確認まで完了

## 細かい修正・整理
* DB保存先移行に伴う文言修正漏れを修正。`.gitignore`にビルド生成物を追加し旧DBファイルを削除

## 開発ワークフローの自動化: 同期・デプロイランチャー
* `sync_deploy.command`を新規作成し、リポジトリ同期・テスト運用フォルダへのコピーを選択実行可能に

## dmg配布タスク6: コード署名・公証 完了
* Developer ID証明書を取得し署名・検証・公証申請・ステープルまで完了、Gatekeeper警告なしで起動できることを確認

## [2026-08-03] (2)
### Changed

## 追加機能 35: ツールチップ・ヘルプ文言の見直し
* 対応形式表示・スライドショー・データベース関連のツールチップを実態に合わせ修正・簡潔化

## [2026-08-03]
### Changed

## 追加機能 33: 自動採番ルール保存時の確認ポップアップ
* 「自動採番ルールを保存」に確認ポップアップ（はい/いいえ）を追加

## 追加機能 34: データベース保存フォルダを開くボタンを追加
* 使用中DBの表示欄にフォルダアイコンのボタンを追加しFinderで開けるように

## [2026-07-30]
### Changed

## 追加機能 28: 重複画像の取り込み可否を選べるように
* 「中身が同じ画像も取り込む」チェックボックスを追加（既定オフ）。判定をDB制約からアプリ側へ変更し既存DBも自動移行

## 追加機能 29: 取り込み順序に「ファイル名逆順」を追加

## 追加機能 30: 検索対象に実際のファイル名（拡張子含む）を追加

## 追加機能 31: フォルダ別グループ表示中はグリッド表示ボタンを無効化

## 追加機能 32: フォルダ別グループ表示の並び順を編集できるように
* フォルダ見出しの右クリックから専用ダイアログを開き、ドラッグ&ドロップで並び替え・保存可能に

## 追加機能 33: フォルダ別グループ表示のON/OFF・折りたたみ状態を記憶

## UI微調整
* 設定画面のコンボボックス・ボタンの高さを揃えた

### Fixed

## フォルダ別グループ表示の状態を復元すると起動時にクラッシュする不具合
* テーマ色決定前に画像一覧読み込みが実行されていたのが原因。テーマ適用を初回読み込みより前の順序に修正

### Changed

## デザインの見直し: 絵文字からSVGアイコンへの統一
* 右クリックメニュー等の絵文字表示を実際のアイコンに置き換え、評価表示の表記ゆれも統一

## 危険操作ボタンの配色を縁取り型に変更
* 削除・リセット系の4ボタンを塗りつぶしから縁取り型デザインへ変更

## [2026-07-29]
### Fixed

## メインウィンドウを閉じても全画面プレビュー・読書モードが残る不具合
* 独立トップレベルウィンドウで自動で閉じない構造だったため、`closeEvent`で一緒に閉じるよう修正

### Changed

## 危険操作ボタン4種の配色統一・ソフト化
* 不統一だった配色を強い赤から落ち着いたテラコッタ系へ統一

## ボタン名・ツールチップの全体最適化
* 全37ボタンを棚卸しし未設定ツールチップの大半に説明文を追加、ボタン名も3件変更

## 設定ダイアログのさらなる調整
* 表示項目チェックボックスをグリッド化、ダイアログ最小幅を縮小、既定の高さをメインウィンドウに統一

## 追加機能 27: 読書モードの中央詰め表示
* 見開き両ページを画面中央で突き合わせて表示するオプションを追加し設定として記憶

## [2026-07-28]
### Changed

## 追加機能 25: 読書モード（見開き表示）
* 同フォルダの画像を本のように2枚並べて全画面表示する新機能を追加（「フォルダ別グループ表示」有効時のみ）。左開き/右開き切替、複数手段でのページめくりに対応

## 追加機能 26: 読書モードの背景テーマ（ダーク/ライト）切り替え
* 読書モード全体の配色切り替えボタンを追加し、アプリ本体とは独立した設定として記憶

## 設定ダイアログのUI最適化
* 縦長だった設定ダイアログを横並び化・スクロールエリア化で改善し、高さ上限を画面の85%に設定

## [2026-07-26]
### Removed（試みたが撤回した実装）

## OSユーザーごとのデータベース自動分離 → 撤回
* 複数OSユーザーでアプリを共有しつつDBだけ自動分離する狙いだったが、`.app`起動時に`com.apple.provenance`拡張属性によりTCCが未署名`.app`からのアクセスを拒否し`authorization denied`が発生。変更一式を撤回し、`~/Library`配下などプロジェクト外の保存先はセキュリティ機能の影響を受けるとの教訓を次の「複数DB切り替え機能」に反映（プロジェクトフォルダ内限定の設計）

### Changed

## 追加機能 24: 複数データベースの作成・切り替え（プロジェクトフォルダ内限定）
* プロジェクトフォルダ内だけで完結する設計で再実装。複数DBを作成でき使用中DBは`current_db.txt`で記録、切り替え時は自動再起動

## 設定ダイアログの調整
* 表示項目チェックボックスを横並びに変更し重複する「閉じる」ボタンを廃止

### Notes（除外設定）
* `.gitignore`に`current_db.txt`を追加、`image_metadata*.db`指定へ更新を推奨

## [2026-07-25]
### Changed

## 追加機能 19: 連番命名規則のオン/オフ切り替え
* オンなら従来通り連番付与、オフなら実ファイル名をそのまま「名前」として取り込む

## 追加機能 20: 画像一覧のCSVエクスポート機能
* 「基本情報のみ」「基本情報＋メタ情報」から選択可能。UTF-8 with BOMでExcel文字化けを回避

## 追加機能 21: 「名前」フォームのツールチップ
* アプリ内だけの名前であり自由に変更・重複可能である旨を表示

## 追加機能 22: ヘルプダイアログの改善
* 専用ダイアログに変更しサイズ拡大・スクロール対応、マニュアルへのリンクを追加

## 追加機能 23: UIボタンの配色見直し
* 「採番リセット」を危険操作の配色に統一、CSVエクスポートボタンに緑を追加

## ドキュメント整備
* `manual.md`を実際の仕様に基づいて大幅拡充。UI配色・文言一覧資料を新規作成

### Fixed

## フォルダ別グループ表示を折りたたんだ状態で画像送りするとアプリがフリーズする不具合
* 折りたたみ中の画像が選択行から消え探索ループが終了条件と一致せず無限ループになっていたのが原因。折りたたみ中の画像は非表示にするだけの方式に変更し、探索ループも件数上限で必ず終了する構造に修正

## [2026-07-23]
### Proposed（未実装のアイデア）
今回の会話で検討したもので、まだ実装していないアイデアを記録しておく。

## 画像一覧のCSVエクスポート機能
* 標準の`csv`モジュールで実装予定（項目・範囲・設置場所は未確定）

## 「名前」フォームのツールチップ追加
* 文言は確定済み（自由に変更・重複可能、初期値は自動採番）

## 対応画像形式の分かりやすい案内
* 「フォルダを取り込む」ボタン・ヘルプ画面に対応形式を明記予定

## [2026-07-22]
### Changed

## 追加機能 17: フォルダ別グループ表示の折りたたみ機能
* 見出し行クリックで画像一覧を折りたたみ/展開（▶/▼で状態表示）、フォルダごとに独立保持。埋め込みボタン方式は不具合のため撤回し見出し行全体をクリック対象に変更

## 追加機能 18: ComfyUI形式のメタデータ対応
* `workflow`/`prompt`のJSONを検出し「その他パラメータ」欄に生JSONのまま保存（従来形式への影響なし確認済み）

### Fixed

## 新規データベース作成時の設定初期値シード処理の欠落
* `init_db()`内の既定値書き込み処理が失われていたため、移行処理を復元して対応

## 生成パラメータ個別表示のデフォルト値の巻き戻り
* 既定値を「非表示（0）」に修正

## [2026-07-21]
### Changed

## 追加機能 14: メタデータ編集欄の表示/非表示切替
* 各見出し行に表示/非表示切替ボタンを追加し、非表示分の高さを残りの欄へ自動配分

## 追加機能 15: 確認ポップアップのアイコン統一
* 標準アイコンから専用SVGアイコンに統一し共通ヘルパー関数`show_confirm_dialog()`を新設

## 追加機能 16: 区切り線の追加
* プレビューエリアと操作ボタン行の間、各メタデータ見出しの上に薄い区切り線を追加

## UIデザインシートの作成
* UI要素の表示名・変数名・objectNameを一覧化した`design_sheet.md`を新規作成

### Fixed

## フォルダ別グループ表示の見出しがライトモードで見えなくなる不具合
* QSSの文字色優先順位によりライトモードのみコントラスト不足になっていたため、見出し背景色をテーマ専用色に修正

## 空リスト/ドラッグ中プレースホルダーの背景色
* 周囲と同化していた背景色を入力欄と同じ配色に修正

## ドラッグ&ドロップでの画像取り込みを一時的に無効化
* 動作不安定のため無効化し取り込みボタンに一本化（コードは温存）

## 標準サイズのプレビュー画像が画像送りのたびに肥大化する不具合
* sizeHintがレイアウト計算に含まれフィードバックループが発生していたため`QSizePolicy.Ignored`に変更

## メタデータ編集欄コピー機能の実装中に混入したコード構造の破損
* 誤って別メソッドに迷い込んでいたコードを正しい位置に修正

### Changed（余白の微調整）
* 「名前」欄まわり・編集エリア・プレビューエリアの間隔を複数箇所で調整

### Proposed（未実装のアイデア・更新）
* 「アイコンのテーマ別配色ルール」は部分的に実装済みに。残りの項目は引き続き未着手

## [2026-07-20]
### Changed

## 追加機能 9: アイコンデザインの本格導入（Material Symbols）
* Google Material Symbolsを組み込み絵文字アイコンを40種類以上置き換え。見出し・ラベル・ドロップダウンにも追加し、アクセント背景上では自動的に白色へ切替

## 追加機能 10: 生成パラメータの個別表示
* Steps・Sampler・Scheduler・CFG scale・Seed・Sizeを使用モデル欄下に読み取り専用表示し、項目ごとに表示/非表示を切替可能に

## 追加機能 11: 命名規則のアペンド欄、組み合わせ別の採番、採番リセット
* 「アペンド」欄を追加しプレフィックス＋アペンドの組み合わせごとに採番カウンタを独立させ、「採番リセット」ボタンを追加

## 追加機能 12: 設定のリセット・データベースの初期化
* 「この設定をすべてリセット」「データベースを初期化」ボタンを追加し実行前に確認表示

## 追加機能 13: デザイントークンの導入
* 余白・フォントサイズ・アイコンサイズを共通定数として整理し統一

### Fixed

## 設定メニュー内、連番桁数スピンボックスの視認性（ダークモード）
* 背景色と文字色が重なっていた不具合を他の入力欄と同じテーマ対応スタイルで修正

## スライドショーボタンのアイコン
* 視認性の良くなかったアイコンを削除しテキスト（▶/■）表示に戻した

### Proposed（未実装のアイデア）
今後の参考として未実装のアイデアを記録。

## UI統一性のさらなる改善
* アイコン運用ルールの明文化、テーマ別配色ルールの拡充、余白ルールの体系化

## 機能面の改善案
* 採番状況の一覧画面、取り込み前の名前プレビュー、設定のエクスポート/インポート機能

## [2026-07-19]
### Changed

## 追加機能 1: 削除した画像を同期対象から除外
* `excluded_paths`テーブルで削除パスを記録し同期時にスキップ、明示的な再取り込みで解除

## 追加機能 2: 画像の編集ロック機能
* 右クリックから画像単位で編集をロック/解除でき、ロック中は削除・編集欄をグレーアウトしアイコンで判別可能に

## 追加機能 3: メタデータ欄にクリアボタンを追加
* 各コピーアイコン左にクリアボタンを配置し、「変更を保存」まで確定しない一時的な変更として扱う

## 追加機能 4: サムネイルサイズ調整（小/中/大）
* グリッド表示のタイル幅・アイコンサイズを3段階から選択可能に（「大」は場所・日時も表示）

## 追加機能 5: フォルダごとのグループ表示
* リスト表示でフォルダ別に見出し行を挿入してグループ表示（グリッド表示中は無効）

## 追加機能 6: 画像リストへのドラッグ&ドロップ取り込み
* Finder等からのドラッグ&ドロップで画像・フォルダを取り込み可能に

## 追加機能 7: 「DB内表示名」→「名前」に変更、新規取り込み時のデフォルト値を連番に
* フィールド名を「名前」に変更し初期値を実ファイル名から連番（例: CG_001）へ、削除しても番号は再利用しない

## 追加機能 8: 歯車アイコン+設定メニュー
* 連番命名規則編集・テーマ切替・ヘルプ・リリースノートリンクをまとめた設定メニューを追加

### Fixed

## 「変更を保存」ボタンの表示が戻らない不具合
* 複数選択時の保存ボタン状態リセット処理が削除時に抜けていたため、削除後にもリセット処理を追加

## f-string構文エラーによる起動不能（Python 3.9環境）
* f-string内のバックスラッシュ記法がPython 3.11以前で構文エラーになるため、バックスラッシュを`{}`の外に出す書き方へ修正

## フォルダグループ見出しのアイコン表示
* 絵文字埋め込みだった見出しをOS標準のフォルダアイコン表示に変更

## 設定アイコンの位置・サイズ
* 分かりにくかった位置から画面右上（「名前」欄の右）へ移動しサイズも拡大

### Planned（今後対応予定）

（現時点で未着手の項目はありません）

</content>
</invoke>

---

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に基づいています。
